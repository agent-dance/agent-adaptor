# T16 R030 live fixture repair

T29-E01 arose because the isolated live fixture retained API-key authentication but discarded the selected custom model provider. CLI 0.153.4 does not apply OPENAI_BASE_URL as a substitute. A shared test-only helper now consumes a versioned, closed routing projection and passes official Config.ExtraArgs overrides to exec and app-server. It never opens the operator profile or copies a complete configuration. The authorized external runner owns the minimal native projection.

The route file has exactly six fields: version=1, model_provider, name, base_url, wire_api=responses, requires_openai_auth=true. The API-key-only seed contains only OPENAI_API_KEY. HTTPS is required except for literal loopback HTTP diagnostics. Missing, duplicate, unknown or unsupported inputs fail with static diagnostics before authentication materialization or CLI launch. Custom provider identity stays inside the TOML value, including literal dots and quotes. Built-in openai uses the explicit official openai_base_url override. Both configured Driver fingerprints and persistent process signatures already include ExtraArgs; production code did not change.

A real dummy-key probe found a second fixture prerequisite: exec refused the empty non-Git temporary directory before any HTTP request. The shared helper now performs bounded git init with empty templates and isolated Git configuration. Both transports and conformance use this same private workspace. The probe has its own explicit environment gate in addition to codex_live and the live environment gate. It is not a model-success or acceptance oracle: its loopback target returns an error and its proxy refuses all forwarding. It preserves evidence of separately denied metadata connections.

Ordinary tests cover strict inputs, minimal auth/resources, official TOML round trips, both transport argv projections, policy/instruction guards, config snapshot and resident signature drift. Existing live append, Skill/MCP/plan/collab, cancellation and cross-Agent Dedicated cold-resume assertions remain intact. The real matrix still needs the combined frozen S and strengthened T23 conformance oracle; owner diagnostics do not accept T29 or any historical requirement independently.

Source and check/evidence hashes, old failures, exact environments and remaining observations are recorded in the external R030 rework_attempt report, not in a fabricated historical DAG result.
