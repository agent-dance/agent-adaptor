# Codex metadata availability recovery

The real S731a908 Subagent diagnostic correlated its parent spawn receiver to a metadata-only thread/read returning formal -32603/read-store Internal/session metadata/`is empty`. One observation was unavailable; no positive identity evidence was rejected. The separate nonce predicate also failed in that run and is not explained or weakened by this repair. Previous diagnostics keep their original evidence limits.

The worker now holds only that exact official error as a candidate for one later metadata-only recovery. The owner explicitly allows it after a formal healthy Completed parent terminal with no turn failure, protocol/caller error or known closed/EOF transport. `seal()` only closes admission. Eligibility is a bounded snapshot of the already admitted receivers still missing valid metadata; complete or rejected/conflicting identity is not re-read. Later lookup metadata passes the unchanged strict requested-ID/direct-parent/catalog-role merge. Invalid or contradictory evidence cannot be healed.

There is one worker, at most 128 IDs, at most 2 reads per ID and exactly one final worker result for each processed ID. Initial work and recovery share the original 2s total terminal settlement, including the final 100ms cancellation/join window. There is no per-ID extension or fixed sleep, no RPC wait in the sole reader, no child-work completion prerequisite and no replay of the parent prompt. Context-abandoned pending never recovers and keeps the existing permanent optional-read pause. A blocked write still requires owner transport shutdown before join; original caller/protocol/decoder/Wait and actual write causes remain observable, with partial audit and no failed checkpoint. Raw retains the first error and all actually observed responses, and cannot change after result freeze.

Official 0.153.4 recorder source opens the rollout file before asynchronously writing/flushing SessionMeta; read-store errors abort before live snapshot fallback. The same native binary's no-model private control was readable before and after an injected empty rollout, but returned this error during injection. The literal can also mean no parseable SessionMeta was found; neither the production classifier nor the docs infer physical zero-byte content or promise recovery.

Coverage includes unchanged-base real stdio Run/resident red (one read, no capability), second valid proof after parent terminal, exact classification, repeated unavailability, wrong ID/parent/role, known identity, failed/interrupted/missing/malformed terminal, cancellation/Close/EOF, 128-ID/result bounds, total-budget cancellation, synchronous blocked write, and late recovery response in the next resident turn with frozen prior Raw. Original cause/Wait/decoder and protocol regression cases remain. The live Subagent assertion keeps its original expression/prompt/context and now prints only boolean predicates and a closed Reason category on failure.

No public API, generated schema, dependency or provider configuration change. The isolated base is 731a9082cbadea35db52679d85d5f43e6ce6ce6e. Owner checks and evidence are external; final implementation acceptance, new S G05/B06 and all live/platform validation remain independent. Central R044/AGENTS/streaming/CHANGELOG belongs to root.
