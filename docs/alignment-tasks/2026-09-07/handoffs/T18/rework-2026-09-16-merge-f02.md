# T18 MERGE-F02 polling partial artifact limit repair

This is the R032 follow-up on base `255e2c18c3f68cccae88a315fc1f2a36d869a1bb`, not a replacement for the sealed T18/G03 delivery. The original task, requirements, owner and independent verifier remain unchanged. The external September 16 `live-repair/handoffs/T18/result.json` records the final source commit, fixed-commit checks and evidence hashes.

The polling `ctx.Done()` branch formed a partial result without applying the existing compact artifact count limit. With a Working task containing two artifacts, the independent public-client fixture reproduced two compact artifacts under explicit `MaxArtifacts = 0`; full opt-in artifacts and the cancellation cause were still present. The repair calls the same `limitResultArtifacts` helper already used for streaming cancellation, normal completion and polling exhaustion.

The previous assumption that preserving a partial task was sufficient conflicts with the published `MaxArtifacts` field contract and R032 / T18-AC04: the compact projection must obey the configured count on cancellation and timeout too. The executable replacement expectation is zero compact artifacts for explicit zero, one for a positive limit of one, and both for nil. Omitted compact artifacts produce exactly one `artifact_result_limit` event with the omitted count and requested maximum, after the original artifact updates and before the single delegation terminal. Full `RemoteArtifacts` remain complete only when explicitly requested.

`TestAlignmentPollingPartialArtifactLimit` exercises public `Delegate` across caller cancellation, the existing wall-clock timeout and the independently owned active execution timeout, each with nil/zero/one limits and both full-artifact exposure settings. The active-budget case advances the existing deterministic clock to 99 ms with a healthy context, then to the 100 ms boundary during polling. Every case retains the task/context identity, partial text/messages, error code and matchable cause; cleanup must call `CancelTask` exactly once with a healthy, detached context bounded by five seconds. Live artifact updates and the single terminal remain observable.

Before the production repair, the new matrix had 12 failing leaf cases for zero/one limits and six passing nil-limit controls (the enclosing test also failed). The immutable independent fixture also failed. Both red logs are preserved externally; no historical tests or sealed review artifacts were changed. The required final checks remain the full original T18 V01 package test and V02 `TestAlignment` race matrix at count ten, under Go 1.27.1 with all three live/E2E/golden-update gates disabled. Independent `review_bridges` acceptance and the coordinator's integration/platform gates remain separate from this author's evidence; this local repair does not claim live-provider or release readiness.
