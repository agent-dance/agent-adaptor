# R030：九项 live 返修、六路独立实施与重新验收

用户于 2026-09-16 批准实施 [完整计划](../../../merge-readiness-plan-2026-09-16.md)。原失败证据保留在外部 provider-live 目录；本修订不宣称问题已修复。基线为 `1c49823fafd2a057be4b0fc6dd96a563e957e2e9`，目标 main 为 `919f140f64f89c80933802840c8878681a85a4d9`。

## 返修派发

[rework.json](../rework.json) 是本次返修覆盖层，必须与完整原 task.json、AGENTS 和计划一同传递。它保留原 47 任务、96 要求、45 提交处置、owner/verifier、历史 DAG 和 G05 九项依赖，仅为已有集成基线之上的六个返修 attempt 规定共同 base 和更窄、不交叉的写域。不能把返修覆盖层当作新一轮 B02/B04/B05 的历史交付，不能伪造原 predecessor gate SHA。

协调者先提交本修订及准备文件，把真实提交记录为外部 dispatch.json 的 prepared_head；六个独立 worktree 均从该 SHA 开始。原 source_base 必须是其祖先。T06/T14/T15/T16/T17/T23 分别拥有九项 finding，未映射 W 编号的项目仍绑定原 check 与公开合同。原 task 的 implementation_steps/acceptance 同步追加，不删除旧项。T06 仅为拆出的根包文件及对应测试精确扩权；共享 helper 如需变更，须先分配唯一 writer、精确修订并重新校验。

返修报告放在源码提交之外，明确 kind=rework_attempt、owner、prepared_head、head_sha、commits、changed_files、逐 finding 根因与修复前后证据、全部原 validation 实际结果、局部文档和 remaining。协调者逐一检查真实 Git diff、范围、命令、测试终局和证据哈希。该报告不会以旧 DAG 的 complete 身份通过原 result 校验器。合流后仍由原 G05/G06 schema 和验证流程验收；历史 G04 base 及完整净 diff 不变。

## 验证预算与诊断

T27/T28/T29/T30 的 V01 包级 timeout 分别为 45m/75m/35m/25m，V02 均为 20m。这些预算覆盖当前串行场景原有 1–8 分钟 context、版本探针及有界 Close：Claude 约 32 分钟、CodeBuddy 不超过 60 分钟、Codex 约 30 分钟、Cursor 约 16 分钟；conformance 两个最多 5 分钟的模型检查另留探针/回收余量。task.validation.command 正式追加该标志，selector、包、次数、逐测试时限、fuzz 时长和断言均不变。collector 可加 -json/-v，不能另改命令语义。

诊断记录真实 exit/signal/timeout、Reason/Cause 类型、正式 terminal、工具/审批顺序、checkpoint、实际 config/data/profile 根及非秘密指纹分量。完整性断言在进程内完成；凭据/账号/完整用户 profile 不入日志或仓库。已授权的有限诊断使用私有 workspace/profile 和最小认证投影，不能算最终平台/live 验收。真实 provider 仍受 build tag 和环境变量双门；普通 CI 不产生模型调用。

## 合流与门禁

独立审查接受六路后合流并同步中央文档；新的源码与文档提交 S 重新执行 G05、T25/T26 原生平台及 T27–T30 全矩阵。任何新修改都重新冻结 SHA，旧失败与旧通过全部保留。新 oracle 必须参与四 provider 最终 conformance。G06 仅在九项问题及新增问题全部关闭、原要求不丢失、同 S 所有必需检查通过后接受。merge、tag 和 release 不属于本轮授权终点。
