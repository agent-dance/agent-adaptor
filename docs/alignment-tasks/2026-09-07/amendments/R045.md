# R045：CodeBuddy live 夹具明确正式 MCP 调用路径

f9a129b 的完整原 T28-V01 保留失败：catalog 场景 28 组正式 tool-use/result 中没有 DeferExecuteTool 或 hosted echo，真实 VERIFY callback 为1，但 MCP Started/Completed 均为0；Task ID、clear、terminal 和 Skill/Subagent 断言已通过。旧731原矩阵的成功记录包含明确的父域 DeferExecuteTool→hosted echo 及匹配成功结果。不能从本轮 callback 推断任意未知工具名、shell/HTTP路径或生产 observer 漏事件，旧失败不改判。

安装的官方 CodeBuddy 2.151.0 已证 ToolSearch 接受 tool_names 中的完整精确名称，DeferExecuteTool 接受 toolName:string 与 params:object，并按正式 MCP schema 路由。原 catalog prompt 只要求调用 alignment_echo；夹具现在明确要求父 agent 使用 native ToolSearch 查询完整 MCP 名，再以 native DeferExecuteTool 提交正式参数并等待成功结果，禁止将这一调用交给子代理或用 shell/scripts/manual HTTP 替代。这是测试所需协议路径的明确化，不是已证明的生产缺陷修复，也不保证模型必定遵从。

T15 唯一源码写域为 codebuddy/alignment_live_test.go 的原 catalog prompt 中这一句。其余 Skill/Subagent/Task/clear 指令、实际 callback、typed MCP Completed、精确 canonical/catalog 关联、真实 task ID、清空快照、terminal、模型、场景数量和8分钟预算均保持。生产 observer、parser、dispatch、正式包装解析、Raw/Transcript、collector 和所有负例不变。禁止凭 callback 伪造 capability、放宽断言、扩大 skip 或重复调用刷绿。

按固定 worker SHA 静态核对只改变该指令，并以禁用 live 门的 tag 编译确认。新合流 S 仍须完整 G05、11个CI jobs、T25–T30 原矩阵同 SHA 重验；Codex 继续使用用户指定 gpt-5.6-luna。此前通过为历史证据，本次失败保留，G06 通过后才发用户授权的 PR，不 merge/tag/release。47任务、96要求、45历史、原G04及G05九依赖和全部验证命令不变。
