# R035：首轮完整G05失败与容量边界返修

候选f7aa53cd8479cdcda4990a80d302a5cca7aac14e的完整G05没有通过：V02记录5008 named
PASS、14既有允许SKIP、3个Cursor失败及E2E temporary_clone父/子失败；其它原命令、
任务/CI Python回归通过。source前后相同、完整逐测试终局、回收均已封存。不得仅清空
collector XDG来重跑报绿，不得把失败降成skip或沿用旧SHA的CI/live结论。

## T17 — G05-F01

原Cursor HOME fallback/Native AuthLink三个fixture必须独立隔离CURSOR_CONFIG_DIR、CURSOR_DATA_DIR、XDG_CONFIG_HOME等宿主变量；保留官方XDG优先级的独立正反矩阵，不删除检测model/config/AuthLink断言，不改production以迁就旧fixture。

## T04 — G05-F02

真实子进程E2E区分Dedicated会话文件保留的跨Agent续接，与已删除temporary profile的安全不兼容：后者ResumeOnly启动前拒绝且不改旧健康record，正常continue-or-start只启动一次fresh并原子提交。保留同Agent两轮resume、完整工具/MCP/Raw/Transcript、旧gateway/token撤销、私有源不污染及cleanup断言。

## T16 — MERGE-F03

Codex child角色表容量只约束新增ID；已登记ID到达128上限后仍必须检查重复/角色冲突，矛盾正式证据不能保留旧角色并报告Completed。保留独立127/128红绿、容量上界、相同role重放幂等、新ID安全拒绝及正式child/receiver双证明。

T17只改既有cursor/**及自身handoff的fixture隔离；T04只改原拥有的e2e/tools_test.go
及自身handoff，不能删temporary分支或只检查任意error；T16只改Codex边界与对应
最小合同测试，不能绕过capacity/事件安全检查。新增test精确路径在最终G05合流时
纳入既有owner范围。三者从本修订外部dispatch指定同base独立worktree实施，保留
各自全部原validation，再加入反例与G05相同隔离env的复验。依次由非作者验收。

临时目录已删除不是可恢复会话；必须以可执行的ResumeOnly拒绝/无spawn/旧checkpoint
不改，以及continue-or-start一次fresh/新checkpoint原子持久化证明正确语义；Dedicated
保持跨Agent真实文件+相同会话身份续接。不得为通过旧fixture忽略实际Cursor根guard。

所有旧报告及失败不覆盖；原47任务、96要求、45历史commit、G05九依赖和原owner/verifier
保持。累计owner不等于并发上限增加，当前至多六实施/审查。修复+中央文档新S形成后
完整G05、T25–T30和G06重验；f7aa53c不再是可用验收候选。
