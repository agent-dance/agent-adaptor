# R020 — T20 独立 post-unlock 故障验收

C04 独立审阅确认 T20 原 QA-06 仅引用旧作者测试，尚不满足 R007 的独立故障验收。原负责人已确认 Claim.beforeLockClose / beforeRootClose 是现存私有 seam，在真实 OS unlock 成功后调用；无需生产或公共 API 修改。

精确新增 T20 ownership：internal/hostedprofile/alignment_release_lifecycle_test.go。仅独立 package hostedprofile 测试，自建真实目录/Claim，注入 close 故障并保持 successor active barrier，核验旧 Release 重试不能覆盖 state/marker/session、不能释放新 owner 锁，错误保持可观察且等待有界。原两条 e2e validation 完整保留，新增 `go test -race -count=10 ./internal/hostedprofile -run TestAlignmentReleaseLifecycle`。

该路径与 B05 其他所有任务不重叠；96 requirement/43 task/DAG/最大6并发保持，不增加生产依赖或导出面。fixture/native OS本地证据不替代 T26 原生Windows no-delete-sharing/DACL 验收。
