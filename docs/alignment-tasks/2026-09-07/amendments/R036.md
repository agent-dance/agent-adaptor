# R036：三项返修合流与新候选验收

R035 的三个原 owner 已交付固定提交：T17 `1a7332f0b36aa266d98237c9691a98fef942b58a`，T04 `d57ac1c537b31492b1fe1070fa3d8bd1cb5f967a`，T16 `b2a907a43e780bf49f96ea341fe07b24eded0700`。非作者结论、红绿与实际合流 SHA 记录在外部执行证据；本文不替代测试结果。

- G05-F01 只把三个 HOME fallback fixture 切到已有环境隔离 helper。原断言、生产路径优先级、官方 split-root 和 Windows 矩阵不变；原 collector 私有 XDG 环境保留。
- G05-F02 在真实子进程 E2E 中区分临时 profile 已删除后的安全拒绝/新会话与 Dedicated 跨 Agent 续接。临时 ResumeOnly 必须在 spawn 前返回 ErrResumeRejected，健康 record/checkpoint 与 Finalize 次数不变；默认调用只启动一次 fresh，并通过真实 memory.Store 的唯一 Finalize 原子 save/archive/rebind。原 MCP、鉴权、工具、目录和清理断言保留，Raw/Transcript 与 provider session identity 检查加强。
- MERGE-F03 的 128 上限只限制新增 child ID。既有 ID 必须继续合并正式角色证据；相同 role 重放幂等，冲突保持粘滞，pending spawn 不得错误 Completed。新身份溢出仍不构造事实，child 与当前 receiver 双证明不变。

G05 仅精确追加 `cursor/driver_test.go`、`e2e/tools_test.go`、`codex/appserver/alignment_child_capacity_test.go`，分别归原 T17、T04、T16。中央 AGENTS、CHANGELOG、使用说明与 G05 片段同步；根 API golden、依赖和 generated 协议不变。

首轮失败 f7aa53c / G05 attempt11 永久保留。只有三项独立验收接受合流、全部本修订文档进入新 S 后，才重新执行完整 G05。不得只重跑失败 selector；全部 T25–T30 与三组 Actions 仍须使用同 S，32 Windows 必过条目和所有原命令/次数/时限/skip 不变。Codex 具体认证授权未获得前不得执行 T29；不能通过文档关闭 G06。

原 47 任务、96 要求、45 历史提交处置、G05 九依赖、G04 base 和唯一 writer 保持。实际 merge/tag/release 不包含在本次授权终点中。
