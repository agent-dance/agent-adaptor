# R023 — 原生 Linux 的 stdin 与 RPC 关闭竞争

T25 attempt3 在 G05 `d94fd17a24f3710a079c79b6932b99a85af6e911` 执行原15 Linux命令及3补充：V01全量4536具名通过、14允许skip，V02vet通过，V03原fullrace exit1，两包因panic失败。V03 4109具名pass、0正常namedfail、12已观察允许skip，另有12中断测试；包panic不能因缺少测试fail终态而被误判通过。后12原命令及3补充未执行。完整V03 JSONL SHA256 `1146c3d9c717cd740646fc3269956f24c5d89bff8c96852011fc9e7e54554b4a`。1055追踪文件host/guest前后哈希一致。

F03：Claude `TestAlignmentClaudeApprovalFailures/question/deny` 在 `internal/clihelper.markDone:177` 重复关闭done。writer defer和Run cmd.Wait后的markDone都是合法调用；现select/default/close在锁外，非原子。T35由原T07负责人只改helper最小生产路径、新stdin生命周期测试及本任务文档。保留Close排队FIFO drain、Write拒绝/取消、partial/cause/checkpoint，不把helper变为parser。

F04：Codex `TestAlignmentCodexStderrAdmission/cancel-held-terminal` 已有真实samebuffer exact14接收证据，取消后固定sourcegraph/jsonrpc2@v0.2.1 reader:235向closed pending.done发送。外部Conn.Close可先close pending但不删map，reader仍可取到该call。T36由原T16负责人仅改client关闭协调、新Client回归和appserver局部godoc。优先切断自有transport并取消activeRPC wait，由原同步reader唯一关闭pending；不得遮panic、放宽T34取消、修改依赖缓存或复制JSONRPC实现。保持caller/RPC cause、FIFO、同一Response、完整Raw drain、健康checkpoint及有界关闭，无handler自等待。

补充T35/T36从同一已验收G04 b2035bc独立实施，精确文件所有权互不重叠，也不重叠已验收T33/T34。B05共九个工作任务，最大同时运行仍6；G05等全部九个交付。原T34测试在G04 worker基线尚不存在，worker不得伪称已运行；合流G05和后续T25必须保留并实跑。中央文档与CHANGELOG只由G05更新。

W04-R05最终owner T07→T35，W04-R07 T16→T36；两项独立verifier T20→T25，真实Claude/Codex T27/T29保留。T20剩22项，原代码/fixture/检查日志不变，仅重新交付metadata；历史24项验收归档。T07/T16其他职责不变，所有96要求、13工作项、45历史提交完整保留。canonical25共47任务（40并行任务+7门禁），DAG与严格后批独立验证规则不放宽。

两项生产要求和G05已重新打开。保留所有旧G05/T25红/绿及诊断源头，新G05按原validator/fulltest/vet验收，B06全部更新至相同replacementSHA，Linux原15+3从头运行。Windows和四provider缺失的环境/授权仍blocked，不靠crosscompile/live skip/CI配置声明通过。无付费调用、依赖升级、push/tag/release。
