# R014 — 预算已选原因不可被取消通知竞争替换

C02对T10固定d623b718267dbae655fa185a4600db2ee4f3b3e0重放原矩阵五轮75通过；新增更窄Stop屏障3/3失败：controller已在锁内确认耗尽并登记bcause=本轮expired，随后parentCancel抢先于锁外b.cancel，使Result/RunFinished改成Cancelled并丢本轮active cause。原始日志/fixture在外部findings/T10-budget-review/final/d623b71，保留此前69和d623所有证据，不以壁钟时间或未执行timer推断先后。

冻结最小私有接口Controller.SelectedCause() error，读取已选cause而不重算parent或修改状态；core/hosttool用各自本轮expired对象身份与已有primary归因。controller确认选择就是已有先因合同的确认点，不允许通知调度竞争改写。标准context取消仍在锁外且遵从其自身规则；SDK不篡改parent/child，只保留唯一公开Reason与原cause。nil-safe、terminal→controller单向锁序，不增加锁内回调。

T10在原ownership内实现并用TestAlignmentActiveBudget前缀及controller fixture纳入现有V02/V03；完整V01–V04不缩减。T18按同一私有合同接线，T22独立回归。42任务、96要求、全部ID、文件范围、并发DAG及公开API不变；更新这些task内容哈希并重新validate，当前T10仍attempt1未被gate接受。原d623不能冒充新最终SHA通过。

C02提交前校核补充：SelectedCause等于本轮expired时，即使child.Err仍nil，也已构成active候选；core不得把它隐藏在contextErr非nil分支中，最终Cause直接保留selected实例。FinishExecution首次结算以已有b.cause先于child.Err/context.Cause，防止后到parent占据标准child cause；已缓存finishErr仍最先返回。SelectedCause不扣时、不读parent、不从Stop合成新原因；parent选择前已Done仍按其Err归因，并非本地预算总是优先。
