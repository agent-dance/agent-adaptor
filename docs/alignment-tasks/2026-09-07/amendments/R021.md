# R021 — 原生 Linux 的 CodeBuddy stderr/Result 交接 race

T25 在冻结 G05 `43e6ba3d90016b1a4b267408ccd200a950041369` 的原 `go test -race -count=1 ./...` 实际检测到数据竞争：`codebuddy/parser.go:509` 的 Transcript append 与 `codebuddy/driver.go:512` 的 Response.Transcript 读取并发。触发为 `e2e/alignment_lifecycle_test.go:271` 的先行健康 Thread.Run，不是后续取消轮。完整 V03 日志 SHA256 `0b206a6e81df0abf7cc68439c31f26f96574c7d42ec2ef4633a7b7aede91a974`；该命令 exit1、4521具名通过、2具名失败、14允许skip。V01/V02原生Linux通过只属于旧SHA，后12原命令和3补充未执行，不冒充完成。

原 T08 部分结果负责人执行新增 B05 T33，从同一已验收 G04 b2035bc 基线修复。精确 owns persistent.go、新alignment_persistent_handoff_test.go、doc.go、T33 handoff，和本批其余五项无写重叠；B05共六项、最多六并发，G05必须依赖全部六项。已有T20–T24源码和验收不删；T20仅按覆盖变更重出metadata，无源码变化无需重复原测试。

W04-R06 的最终 owner 从 T08 转 T33；后批独立 verifier 从 T20 转 T25，T28真实CodeBuddy验证保留。T08原实施、T20原独立fixture及canonical22报告完整归档，T20原场景仍在完整测试树，不删测试。全部96要求、13工作项、45历史提交ID保持；现44任务（37并行工作任务+7门禁）。独立verifier必须晚于owner的严格检查不放宽。

修复必须关闭每轮stderr callback准入并等待已登记callback结束，再finalize和发布结果；保持已观察stdout/stderr、Transcript、Terminal、Usage及部分Result。不得只锁读取、串行化消费者、缩小命令、跳过race、重放prompt或放宽checkpoint。健康常驻只等待本轮callback，不等待整个进程退出；取消/失败仍有界，锁不得圈住宿主callback。无公共API/依赖/golden/正式协议变更。

旧G05验收与Linux红证据保留，implementation_head撤销冻结，G05需合入T33后按原validator/fulltest/vet重验新SHA。B06仍只读取同一replacement SHA，全部原15Linux命令及3补充从头执行；Windows/live未获得的环境/授权仍是真实缺项，不改成通过。
