# R017 — 补齐冻结前的真实协议验证入口

T23 在已验收 G04 `2421fe470cf67b22697fef796b038c0be6e395c8` 只读检查 B06 场景时发现未完整覆盖的 live fixture：CodeBuddy、Codex、Cursor 缺少 Dedicated+WithTools 的跨 Agent.Close→新 Agent ResumeOnly 会话 nonce；Claude、CodeBuddy 观测用例未对已声明 Skill 与 canonical Subagent 完成事实断言。

这是验证入口覆盖缺口，尚未运行付费/真实 provider，不能描述成已复现的 provider 生产失败。原 G04 全仓3729通过/14允许skip/vet及独立组合44+220和owner重放20的准确SHA与结果全部保留；这些结果不替代缺少的真实场景。

原 owner T14–T17 在原 G03 base 和各自已验收分支上只补各自 live fixture/测试辅助与交付片段，完整原验证仍须在新 final HEAD 执行；tag+live env双门保持，普通检查禁用live。跨Agent用例只首轮提供随机nonce，后轮不能重新输入nonce，使用真实store/Dedicated profile/WithTools，先确认工具实际调用、健康checkpoint，完成有界Close后新Agent按同key ResumeOnly，验证原session身份/历史nonce；稳定构造维度与安全资源轮换必须准确。没有真实运行授权时只有静态/编译/双门证据，不填live通过。Cursor保持每轮spawn、不添加persistent/fork/Skills/Todos能力。

Claude、CodeBuddy 新能力用例物化明确已知Skill与Subagent资源，并要求正式ProviderProtocol、准确canonical key、Completed成功；不能用资源声明/native输入接受/assistant自述代替。保留现有MCP/Todo/嵌套父关联断言。原生不支持或没有正式证明时用例失败，不伪造/skip通过。任何生产修复需另报具体反例给root，不把补测试暗中扩展成实现重构。

协调者重新打开G04门禁及T14–T17交付，保留pre-R017接受报告。B05五个互不冲突的QA/doc工作可继续在原2421fe4编写；正式完成前必须迁移到重新验收的G04 SHA并执行原完整验证，不能沿用旧基线测试报告。全局最多六个worker同时执行，返修按空闲slot派发。任务42、要求96、ID/ownership/执行DAG/原validation保持不变。G05不在遗漏未补或新G04未接受时冻结；B06仍要求同一最终implementation SHA与明确真实调用授权。
