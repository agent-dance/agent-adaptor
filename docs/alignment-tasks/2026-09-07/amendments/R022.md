# R022 — Codex 常驻 stderr 验收的接收前提

T25 在 `45cada4315a6a338071452995918d2292e2c9b28` 原生 Linux 原 V01 fullcount1 实际失败：4531具名pass、2具名fail、14允许skip，日志 SHA256 `98f67cfb2fb8f46cc761df9c85517a9855623d996793f7a554b84aa5e0357e89`。失败为 `TestAlignmentPartialResultAndPublicEquivalence//thread-true` 的首次健康 Stream；原 fatal 合并 stderr/Usage 四项而未打印值，不能只凭该日志定性。后17条原命令未执行，1051源码哈希未变。隔离诊断只增加实际值打印、不改原谓词/生产，复现同型stderr为空且Usage=0/2；另以首次Read前暂停与真实同buffer接收正控证明接收前提不足。原始未打印值的那次失败仍不倒填字段；详细实值及hash见协调者 R022-diagnostic-decision。

C02裁决：stderr独立pipe的child write完成和stdout terminal都不能证明parent已接收字节。已接收Raw必须完整保留；未来未接收字节不得推断归属或补写已返回Result。正常持久进程不能为一次健康turn等待整个进程退出。用真实child、parent私有buffer接收gate保留exact oracle，并保留公开Response→Result、Run/Stream、Usage/Terminal与稳定性覆盖，是纠正不可成立的测试前提；仅删除exact断言或允许空值不构成闭环。失败/one-shot的EOF drain exact断言仍在。

新增 B05 T34，由原T16 owner在相同G04基线只改精确测试与godoc，不改生产、公共API、依赖、golden或生成协议。通过C02/C03独立审阅及原完整包/race5、新gate race20/vet后才可合流。T34没有新增implementation/verification要求声明：W04-R07原owner T16、verifiers T20/T29及全部96要求、13工作项、45历史提交保持。现45任务（38工作任务+7gate），B05七项、最多六并发。

旧45cada43 G05和T25-F02原始失败保留，G05依赖全部七项后重跑原命令；所有B06检查使用新冻结SHA，T25全部15原命令及3补充从头执行。Windows/live缺少环境或授权仍明确blocked，不把旧SHA/fixtures/静态检查转换为完成。
