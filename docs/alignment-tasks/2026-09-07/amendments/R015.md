# R015 — S9 同批迁移到已发布的统一事件接入

G03第一次固定合流372a46588382e3d083d70a90f7de4546427cf185：任务包校验通过，全仓3141 pass、14个原允许skip；唯一TestServiceTeamCollaborationS9因无绑定的Merge返回ErrEventInjectionUnsupported失败，vet未执行。原S9在leader.New没有装team.Option而使用运行后Merge，违反C03已明确的breaking接入迁移。

T12只新增hosttools/a2adelegation/service_s9_test.go写入权，迁移到运行前team.Option及直接消费原Stream；fake HTTP leader如实声明MCP能力。保留三成员排序、文本delta、结果记录先于terminal、HasLine、Observe及Close全部业务断言。已消失wrapper的等式可改为原Stream RunID非空/稳定。Merge未绑定拒绝合同与回归不变；禁止伪造RunEventsBound、skip/删S9或提前写T18生产接线。

新增T12-V03完整hosttool包与V04 S9 race五轮，原V01及原补充T12-RACE/T12-VET范围保留；42任务、96要求、ID与批次DAG不变，无同批写冲突。T12 attempt2原证据归档后以attempt3返修，原G02 base不变；新最终SHA复验并合入后完整重跑G03。C03独立逐块比对S9断言，后批T18仍负责真实BindEvents/publisher/bus证明与跨层事实。缺失并未转换成通过。
