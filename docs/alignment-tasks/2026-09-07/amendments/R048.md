# R048：CodeBuddy live 夹具保留原生登录语义

用户确认终端直接 `codebuddy` 可以使用。6934126 的 T28 attempt16 使用测试进程继承的 API key，V01 十一项真实场景均记录 `401 Authentication required` 和 `token-type:ApiKey`；V02 两项真实调用失败，其诊断中的 stderr 字节数 401 不能当作 HTTP 状态。终端启动文件未设置这些继承变量，不能把测试进程的 API key 当作用户正常终端的认证来源。旧 token-only 投影还改变了官方 storage 选择并丢失刷新语义。所有旧失败保留，不以这次调查倒填 cold exact-text 或 catalog deadline 的历史根因。

当前官方 CodeBuddy 2.157.0 从 HOME 下的平台专用 `CodeBuddyExtension/Data/Public/auth/Tencent-Cloud.coding-copilot.info` 读取原生会话；`CODEBUDDY_CONFIG_DIR` 不控制这条路径。原 live 夹具只复制两个 legacy credentials 文件，却另设空 HOME，不能交付该原生登录态。单次独立诊断已证明：完整逐字复制正式会话到私有 HOME，去掉继承的 key/token/route 覆盖，由官方恢复会话后，`glm-5.2-ioa` 返回唯一健康终局和精确诊断回复；原文件未改变，临时副本已删除。这只是认证诊断，不替代原 T28 矩阵。

原 T15 负责测试私有的 `CODEBUDDY_NATIVE_AUTH_FILE_SOURCE` 和统一 HOME/profile 准备。实际 Agent、两代 cold Agent 和 conformance 必须把完整 opaque seed 放在其实际 HOME 的正式位置；不解析、重编码、解密或转换 token，不复制业务 settings/MCP/skills/agents/hooks/history。显式 native 输入缺失、损坏、链接或与另一认证选择冲突时，在 CLI 前明确失败；不静默退回其他凭据。保留明确的 env 认证方式和原双门。禁用的 conformance 不读取原生凭据。

副本在私有目录中创建，写入前已经受保护；Windows 使用实际 owner/DACL 验证，不能用 Chmod 或交叉编译代替。provider 合法更新私有 `.info` 的能力必须保留，关闭 Agent 后才清理其 HOME，cold 两代共享该生命周期。源文件不得被修改或删除。普通测试只使用合成凭据和固定假 CLI，不读取用户登录数据。公共 API、生产认证和 Driver 执行管线均不扩展。

manifest revision47 只扩充本修复的 G05 精确测试文件与修订文档范围。原 47 任务、96 要求、45 历史、九个 G05 依赖、真实 G04 基线及所有原检查保持。原 owner 完整验证和独立验收后才合流；新 S 必须重新通过 G05、原生平台与全部 B06，G06 通过后才发 PR，不授权 merge/tag。
