# R003：按真实资源时序修正实施边界

修订4，B01执行期代码审阅；生产checkout仍基于同一已验收G00 SHA。协调者以本修订后的完整task派发范围，worker不把任务包变化混入生产提交。G01统一提交本修订，其他任务不读取peer代码。

1. T31增补stream.go、options.go、adaptertest/suite.go。基线wiring.resolveRun在acquireRun后才做schema协商，不满足资源前失败；openStream需保存本次eff私有的唯一预解析结果，wiring消费，不重复解析。suite接入逐Kind蕴含守卫。Stream.Result godoc同步已冻结C02，仍无T05新符号依赖。与本批其他owner无路径交集。
2. C01 §7“claim早期snapshot+后续纯投影足够”在动态skills上不可实现：runservices.claim早于wiring.ResolveSkills/InjectSkills；ResolvedSkills.Fingerprint仅路径/声明并非内容证据。禁止二次解析、跳过未知资源或吞IO错误来凑通过。
3. 保留T04锁、manifest、seed、Close、MCP轮换、session文件冷续接实施。W02-R06最终owner改为B02统一管线owner T06，在G01后消费T04真实实现；增加tools.go、invocation.go、alignment_profile_test.go、skillruntime/mcpruntime范围，无B02路径交集。动态skills/普通资源首次物化后的完整兼容闭环由T06-AC04及重复race验证，不假称T04已经完成。
4. R003覆盖C01 §7的早期快照/纯函数限定：最终snapshot在本轮skills解析和注入完成后、Thread fingerprint/Driver前获取；可使stabilizer返回error，唯一invocation处理。每run不可变视图，不能用跨run共享可变profile缓存。完整指纹维度、未知内容保守拒绝、所有权和单writer约束不变。

原95要求全部保留，W02-R06的独立verifier不变；42任务/7批次数不变。T04剩余要求未删除，T06新接线任务不得遗漏；发布依旧取决于G05与G06实际验收。G00报告仍仅代表当时冻结与源码测试，不倒改其SHA或证据。
