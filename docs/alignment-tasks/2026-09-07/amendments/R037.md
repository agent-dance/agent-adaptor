# R037：原生 Windows 失败及外部 CLI 生命周期审计

候选29e569c的G05通过，但原生Windows两组job共同发现
`TestCursorRuntimeRootsStaySelectedAndCleanupFailureRetainsResponse/Native_runtime_override`
没有checkpoint，原输出仅`Native execution failed <nil>`；不能倒填未输出的exit或协议原因。
Dedicated_cleanup_failure通过。T26-V01实际5041 PASS/18允许SKIP，mandatory31/32，
其余X01–X05 blocked；保留Actions run35098947752和Go35098948096原始artifact/log。

原T17继续独立worktree实施T26-F01，cursor/**与自身handoff写域不扩大。
先确认fixture/退出/正式协议/guard根因；未证明前不得改生产迁就测试。
健康Native必须保留成功checkpoint、实际HOME/config根不被Runtime Env重定向；
Dedicated cleanup error仍保留完整Response且没有健康checkpoint。旧反例和全部32个
Windows mandatory保持，不能通过skip或宽松错误条件关闭。

同S的Cursor T30-V01七个必需root实际PASS，但外部runner把CLI安装目录的
动态`.running/<pid>`空文件也纳入程序哈希，postflight停止后续V02。
源码哈希未变；只读剔除本轮新增空PID标记可精确重构原293文件哈希，
程序字节漂移与正式CLI运行标记必须区分。协调者单独保留旧runner/失败记录，
新版本只在证明正式语义后分开审计这些运行标记；其他代码、资源、版本、链接、
异常标记仍严格拒绝。标记本身不是进程回收证明。未执行V02不补PASS，最终新S
必须重跑完整T30；本外部collector修订不增加SDK API或改provider合同。

原G04 base、G05九依赖、47任务/96要求/45处置和最多六并发不变。
最终修复及文档形成新S后重新G05、全部平台/CI/live；已有通过不跨SHA挪用。
Claude/CodeBuddy/Codex具体敏感出站授权仍按自动审批结果等待用户确认。
