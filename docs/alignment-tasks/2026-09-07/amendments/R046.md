# R046：Windows profile 夹具的启动屏障、失败清理与未知超时

5d5b912 普通 Go/windows 全量失败必须保留：IdentityEncoding 第七身份的本轮8秒预算耗尽，同步Run在14.566秒后以DeadlineExceeded返回；前六操作总计约8.87秒，不是旧共享预算问题。CrashRequiresRecovery未在父观察窗口内记录prompt。日志没有启动、文件IO、协议或取消排空的分段轨迹，不能把两者归于同一根因。专门T26的5504PASS和原32必过均成功，但不覆盖普通job两失败及随后五项未执行检查。

## 可证明的夹具修复

contender直接使用已有测试可执行文件，不逐轮go build/go run。Crash原流程在cmd.Start后立即写输入并开始4秒观察，未证明子进程已进入fixture；它还在观察成功之后才执行stdin Close/Wait，提前失败缺乏及时清理。只在该Crash的test-only子进程参数启用ready→input→held协议，父在有界ready后才交付输入并开始原观察。其他contender模式的协议和结果保持。

父总8秒仍包含启动与等待，子Run4秒和prompt观察4秒不延长。cmd.Start成功后立即登记stdin关闭、取消与唯一Wait归属的有界清理，不能把发出取消当成完成回收；成功路径仍须真实held后关闭输入并等待约定退出，再检查crashed claim/state、ErrRecoveryRequired和绝不重放。缺失ready/held、早退和超时继续失败，cleanup不得吞原cause或泄漏进程。

## 未知超时与验收边界

IdentityEncoding保持全部七身份同时持claim、完整Run8秒起点和canonical alias冲突断言，仅在失败时增加安全ledger种类计数、部分Result是否可用及Raw/Transcript长度或闭集种类诊断。不得后移计时、扩大预算、换成假成功或声称其根因已修复。

T20唯一默认写域e2e/alignment_profile_test.go，生产代码、共享deadline、CI并发、provider、原矩阵/skip均不变。先保留只读分析，再用确定性ready前延迟反例证明旧观察起点误拒绝及新流程在原总预算内成功；缺失/超时/早退和早登记cleanup独立验收。结果必须在新S的原完整Windows及平台矩阵重新检验，单场景或专门T26成功不能覆盖原full失败。

R045 CodeBuddy fixture真实场景在5d本轮被401阻止，不能宣称路由澄清已通过live。Cursor执行失败与本机status的本地凭据存在语义亦不能互相替代。保留原失败，凭据/环境问题不编造成SDK缺陷。新完整S重验原G05、11CI jobs及全部B06；G06全部通过后才发PR。47任务、96要求、45历史、G04与G05九依赖及所有原命令保持。
