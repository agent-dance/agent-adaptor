# R031：返修诊断闭环、兼容边界与合流验收

本修订落实已批准 R030 计划中的新发现登记和集中合同同步，不把有限诊断或 owner 测试当作最终验收。原 47 任务、96 要求、45 个提交处置、所有原 owner/verifier、G05 九项依赖和历史 G04 base 全部保留。原九 finding 不删除；下面三项及其后的 MERGE-F01 追加到同一个 rework overlay 和对应原 task 的执行/验收要求，不伪造 W 编号。

| 新条目 | 原 owner | 最终独立检查 | 已证事实与修复边界 |
|---|---|---|---|
| T29-E02 | T16 | T29-V01/V02 | Codex exec 拒绝非 Git 私有空目录；共享 live helper 有界初始化无 template 的私有 Git workspace，隔离 Git 环境，不把 exec 专属绕过参数传给 app-server。 |
| T30-F03 | T17 | T30-V01 | Cursor print 的公开文本事实是 assistant NoticeTranscriptItem；原 TextDelta 取消前提不可达。必须收到本轮非空正式 assistant 条目才 Cancel，保留取消 cause、Reason、部分审计、旧健康 checkpoint 和有界收尾。 |
| T30-F04 | T17 | T30-V01 | 官方 opaque call ID 可含换行；为 capability 身份做稳定无碰撞编码，保持 start/result/parent 关联与 Raw/Transcript 原文，不以裁剪或丢弃一段掩盖。 |

T30-F01 的正式 MCP 冗余字段校验同时接受已支持的 plain toolName 和实际已证的 exact serverIdentifier + '-' + toolName；仅作同一正式对象的一致性校验，不以拼接字符串当跨域身份或递归猜测 operation。错型、空/null、未知 server/name 仍拒绝。

## 已落实的合同与保守兼容边界

T06 的 Claude 2.1.159 初始化修复只归一化已完成 version13 的完整六字段组合，精确条件和旧记录迁移边界见 C01 §R031。真实配置、资源、模式、manifest 证明和未知/未完成初始化不被隐藏。旧 completed-bundle hash 记录可能保守拒绝 ResumeOnly；健康记录及会话文件保持，不增加双指纹、第二执行入口或 store 重写。

T14 修复 live fixture 的精确私有 ask 前提，实际请求与工具结果必须相关联；不伪造 Permission。T15 保持 Question 独立 transport 选择并只在明确 native JSON transport 解析完整正式顶层记录；错序/畸形/失败没有健康 checkpoint。T23 的 live 探针必须获得健康成功和明确请求的值，普通 SPI 失败/空响应仍合法。T16 只修显式路由、最小 auth seed 与 workspace 测试前提，不改生产 API 或 generated protocol。

R030 的 Claude 包级预算说明更正为原串行测试 context 合计约35分钟（此前文字写约32分钟）；正式45分钟包限额、全部逐测试 context、selector、次数和断言均不改。其他三 provider 预算与原 V02 命令保持。

## 合流与最终证据

六路仍从 prepared 68c30cf445729a9ab94b1b5ad7b50c1f5877579e 独立交付；返修报告 kind=rework_attempt，不伪装历史 DAG complete。非作者接受后由协调者按真实 diff 仅以精确文件追加 G05 整合范围。公共合同、godoc、使用文档和 CHANGELOG 必须进入最终 S；API golden 未变，不自动生成扩大公共面。原冻结合同清单须同步文件哈希。

G05 完整原命令通过后，T25/T26 Actions 与四个本机 provider 原完整矩阵都使用同 S。新的任何源/受检文档修改都形成 S' 并重验；旧失败与通过封存，不改成当前结论。pending 授权/真实能力不足保持未通过，普通 CI 永不读凭据或付费。G06 只在原九项、新三项、MERGE-F01 及后续实际发现全部关闭后通过。实际 merge/tag/release 仍不在本计划终点内。

## MERGE-F01：完整合入审查重新打开安全回退事件边界

非作者 core 审查使用同一公共 Thread/RunService fixture：919f140 基线通过，646a26d 候选在 resume 拒绝的 Driver terminal 后安全 fresh 成功时丢失全部新文本事件，且公共 terminal 保留旧 provider ThreadID。无拒绝 terminal 对照两版均通过；无 RunService 的早期历史失败另留，不能充作 introduced 证明。独立 fixture SHA256 为3185ce8527702d11d2468b6d1fca144a6d0f21bcecf3613eca8a19a6c3b49135，原红日志保存到外部 final-core 审查目录。

原 core lifecycle owner T06 在同一 prepared base 的既有 worker 追加修复，仅增加 sink.go、invocation.go、alignment_resume_fallback_test.go 三个精确写域（及既有 own handoff）。不由 gate 修改生产，不丢弃已审查的 Claude 修复。新 attempt 仅在原安全 PrepareFresh 成功后重置 per-attempt terminal/source/坐标；同 attempt 终局后的迟到数据继续拒绝，一条公共生命周期、原累计 Raw/Transcript/Usage、失败cause和健康checkpoint保护均不变。原 T06 全部 V01–V04、新独立反例及最终 S G05/B06 必须重新接受。该修订登记问题，不宣称已修复。

本次五路已审查合流相对历史 G04 的 G05 精确新增整合路径（Cursor及后续fix在实际交付时另核对）：

- `claude/README-streaming.md`
- `claude/alignment_permission_test.go`
- `claude/interactive_live_test.go`
- `codebuddy/alignment_live_policy_test.go`
- `codebuddy/alignment_native_json_test.go`
- `codebuddy/alignment_question_policy_test.go`
- `codebuddy/driver.go`
- `codebuddy/engine.go`
- `codebuddy/live_protocol_test.go`
- `codebuddy/native_json.go`
- `codebuddy/parser.go`
- `codebuddy/persistent_test.go`
- `codebuddy/run_live_test.go`
- `codex/alignment_live_routing_test.go`
- `codex/appserver/run_live_test.go`
- `codex/conformance_test.go`
- `codex/internal/livetest/profile.go`
- `codex/internal/livetest/profile_test.go`
- `codex/live_profile_test.go`
- `codex/live_route_probe_test.go`
- `tools_profile_snapshot_test.go`
