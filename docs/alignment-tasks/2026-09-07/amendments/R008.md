# R008 — G01 全仓 E2E 夹具同步 Dedicated 持久语义

G01 在合流 c2a2d86e9109f619a74fe391976de84533706414 实跑 go test -count=1 ./...，2266 个通过、14 个明确 opt-in/不适用 skip，但 TestHostDefinedToolsEndToEndThroughRealProviderProcess 失败：Dedicated source 不存在；其后续断言还假设 Close 删除 Dedicated clone 和重建必须随机目录。这些旧前提与已冻结 C01/T04 冲突，不能降低生产所有权合同来迁就测试。

将 e2e/tools_test.go 的独占范围加入 T04，必需检查新增 go test -count=1 ./e2e。保留真实子进程、MCP discovery/list/call/auth、两轮同 Agent 及换 endpoint/token 的第三轮，明确建立宿主 source。扩充持久 Dedicated 与隔离的临时选择两种情况：前者 Close 保留 session 和同 namespace，后者删除旧 clone 并重建新目录；两者 source 不污染、旧凭据撤销。不得访问用户原生 profile、删原安全断言或改成纯 mock。T04 原 cold session nonce fixture 保留，独立 T20/T26–T30 仍不算完成。

没有新增 W requirement 或 task：原 96 要求、42 任务、7 批次及最大并发 6 不变，W02 既有 profile/Close 要求覆盖该集成验收。没有其他 B01 worker 拥有此路径。T04 基线仍是 G00，不读取同批 peer 未验收代码；返修在原分支追加可追溯提交，G01 再合入并重跑最终 SHA。先前 G01 和 T04 报告/日志保留为 attempt 1，不作为本轮通过证据。
