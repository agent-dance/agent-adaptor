# R040：dab 原生 Windows 与真实 CodeBuddy 验收后的精确返修

保留 dab1a24 原证据：G05六项通过、5112 named PASS/14原allow；11个CI jobs为10通过和完整Go/windows失败，专门T25/T26绿色不覆盖独立失败；四provider原8命令中Claude/Cursor通过，CodeBuddy/Codex未通过。Codex当前消息仅hash，早前独立429不倒填本轮原因，不换模型/endpoint或重复刷绿。全部47任务、96要求、45历史、G05九依赖及真实G04保持。

## T06：目录别名须以双目录身份作证

Windows Dedicated+managed两分支与Drift四分支在首轮invalid managed skill path，Native控制通过。log显示8.3临时祖先，但未打印实际两个路径，具体8.3展开仍是推论。公开零CLI反例直接证明目录SameFile但lexical Rel拒绝：canonical Dedicated和aliased Native通过，aliased Dedicated失败。

仅改internal/skillruntime/profile_clone.go，以held profile root及直接skills root的双SameFile证明后创建只读manifest Path视图，不以叶链接同target证明父身份。源manifest、canonical ownership与fingerprint不改；直接子项、唯一性、hash、实际link目标、两次有界快照、copied保护和全部未知/替换拒绝保持。测试限profile_clone_test.go/alignment_managed_clone_test.go。新S原生完整Windows必须通过。

## T15：正式包装观测与未知冷续接分量分开

cold A实际callback、checkpoint、会话文件和Close已过，B nil error后的text/callback/phase/terminal联合失败未打印分量。只加安全布尔/计数诊断，不能预定cold生产修复或放宽精确回忆。

catalog的Skill/Subagent正式join、实际host callback、真实taskID和clear已过，缺MCP Completed。固定官方2.151.0及零CLI对照另证明DeferExecuteTool/toolName/params的正式MCP包装被observer遗漏，不能推断该live用了此路径。仅observation.go识别正式一层结构与唯一catalog，匹配成功result后才Completed，Tool/Raw/Transcript原样；未知、嵌套、畸形、错误不伪造事实。测试限alignment_observation_test.go/alignment_public_observation_test.go；安全诊断限两个原live文件及alignment_live_diagnostic_test.go。parser/streaming_parser/driver dispatch和cold生产路径不在本次写域。

## T20：两个场景保持每操作原8s预算

Ownership在已验证owner/同进程/子进程冲突后的other.Run超时；IdentityEncoding某一身份Run超时，原log无索引。原全场景共用8s，原生失败环境与专门T26的OS/TEMP形态不同，不能推断具体慢因。仅alignment_profile_test.go两场景同步分阶段至多8s并记录stage/elapsed，父总预算仅协作取消；无重试、global helper/SDK policy不改、七身份同时持claim。无未join watchdog goroutine；go test timeout/CI监督继续提供测试进程边界。原失败保留，新S完整Windows及原重复命令必过。

## 合流纪律

原owner三个写域互斥，各自handoff可写。root独占中央AGENTS/taskpack/godoc/使用文档/CHANGELOG。独立复验后新S完整G05/B06/G06，所有原命令、次数、allow skip和Windows32不减少。一次诊断只证明其自身；凭据授权对相同目的地/模型/最小投影持续有效，不伪造新授权时间。全部门禁通过后发PR；merge/tag/release未授权。
