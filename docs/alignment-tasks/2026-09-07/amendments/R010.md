# R010 — core 生命周期统一后的背压 fixture 范围补齐

T06 的完整开发命令 `go test -count=1 . ./driver ./capability ./todo ./internal/capabilityobs ./internal/todoobs ./adaptertest` 暴露 5 个 `stream_backpressure_test.go` 旧断言：第一个事件必须是 TextDelta、6 个 delta 等于总事件 6、blocking 16 个 delta 等于总事件 16、Driver 不主动发事件则空流。这些与 R006 已冻结的所有执行由 core 发布唯一 RunStarted/RunFinished 冲突。另一个 runservices 旧断言需允许清理期间已有 RunStarted，但仍禁止释放前终局或关闭。

本修订仅把 `stream_backpressure_test.go` 加入 T06 allow，并要求显式核对新生命周期及其对普通 buffer 的占用。保留原来的 delta 存活/顺序/聚合计数、marker-before-critical、blocking 无丢失、关闭后发送 no-op 和释放后才关闭合同，不能删除断言或仅过滤所有生命周期来掩盖时序问题。`runservices_test.go` 已在 T06 范围内。

无生产路径扩张，无任务/工作项/依赖变化；42 个任务、96 条要求均保留。原失败日志由协调者保存在外部 findings/R010。修订后的原 T06-V01 完整命令必须在最终源码 SHA 实跑通过；G02 再跑整仓集成检查。G01 的历史结果不倒改。

R006 的“所有运行”指已经进入 `executeInvocation` 的准入执行（包括其资源前/资源阶段错误），不改变 `openStream` 静态拒绝的既有 `closed Events + Result error` 合同。closed Agent、无法生成 RunID、非法配置/Tools/policy/schema 在准入前被拒绝，没有 Driver 或资源；不能为可能无有效 RunID 的拒绝伪造执行生命周期。T06-AC05 的 Driver-only/有sources、成功/失败/取消及cleanup主Reason场景全部保留，静态失败继续以零资源/稳定error/空Events对照验收，不增加stream.go范围。G02 使用文档和 T20/T21 独立核对区分拒绝准入与已开始运行。
