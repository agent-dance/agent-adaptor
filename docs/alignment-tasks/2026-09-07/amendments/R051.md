# R051：MCP 输出对象兼容投影

S42 的一次网关诊断仍在原 8 分钟时限失败，但实际观察到 17 次鉴权通过、14 次 tools/list 正常返回且均含 alignment_echo、零 tools/call 和零宿主 callback。服务端返回不等于客户端接受或已建索引；一次本地鉴权拒绝的原因未记录，不能关联旧 provider 登录 401。原矩阵、D1/D2 和更早失败均保留，不倒填未观察字段。

离线反例使用原 catalog echo Definition、真实 Go gateway、显式 2025-06-18 初始化与实际 HTTP tools/list/tools/call 响应。S42 发布 outputSchema.type=string 和字符串 structuredContent；固定 CodeBuddy 2.157 的官方 ListToolsResultSchema 与 CallToolResultSchema 分别拒绝这两个字段。其官方 HTTP 列表解析和索引方法在该响应下进入 degraded 路径，未提交工具索引；将全局 defer 设置为 true 不能绕过解析失败。受控协议版本与错误机制不冒充旧 live 的握手值或内部日志。

原 T02 在私有 MCP transport 修复两项 wire 投影。旧客户端兼容的显式根 type=object 保持原 schema、structuredContent、text 与旧指纹；其他合法输出 schema 固定映射为对象 envelope，structuredContent 为 {"result": 原值}。Definition.Invoke、公开 Descriptor、原输出验证和原 MCP TextContent JSON 字节保持；不删除 output schema，不拒绝既有 scalar/array/null/union/boolean-schema 能力，不根据每次返回值改变目录形态。独立官方解析器还拒绝 input/output 根的直接 properties 中合法的 true/false 子 schema。只在该已知 schema 位置将 bool 映为 {"allOf":[bool]}，保持对象结果不包装、true/false 的原语义和嵌套数据；不递归转换更深层 schema 或数据。新 envelope 的 boolean result schema 同样适配。

嵌套 schema 必须保持 local refs、$defs、$id、anchors 和 dynamic refs 的资源作用域，禁止扫描或改写 const/default/examples 等数据中的同名键。原 schema 与 wire schema 的关系由独立有效/无效值对照验证；实际 wire input/output schema 进入既有 fingerprint，完全无需转换的对象目录保持旧指纹。固定 envelope 开销有界，原 validated output 大小预算、输入纠错、错误脱敏、取消、callback、生命周期和 checkpoint 保护不变。

T02 保留原同包测试、race、根包检查并追加相关 vet；保留真实旧 wire 红例，以修复后真实 HTTP wire 通过固定官方解析器及结构校验作正例。独立 verifier 必须覆盖引用资源、boolean schema、并发/不可变快照、边界值与原输出逐字保留。原 prompt、模型、目录场景时限和全部 callback/typed 事实断言不变，不能以只改 fixture 或 pin defer 关闭此生产缺陷。

manifest revision50 精确纳入本次六条实现、回归、godoc/owned handoff 路径及修订文档的 G05 范围/required reads。47 tasks、96 requirements、45 history、九个 G05 依赖、真实 G04 基线和全部原检查保持。R050 与 R051 合流形成新 S 后，完整 G05、全部 B06 与 G06 均重新验收；实现或受控正例不等于 live 已恢复，不授权 merge/tag。
