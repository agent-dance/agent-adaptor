# R013 — 审批描述快照与应答权分离

B03 固定 T12 2778eff 独立复核实证发现，SSE、AGUI 和 session recorder 的审批描述仍共享 Choices/嵌套 Details。原始失败 fixture 和日志位于外部 findings/T12-review，T12-F03 由原 owner 在其原范围修复。继续只读追溯固定 G02 75332ea 发现根包 cloneEventValue 没有 ApprovalRequest 分支、stampEvent 仅浅复制结构体；newApprovalRequest 只浅拷贝 Payload map。这与 C03 既有“接收者持独立可变副本，responder 保持 exactly-once”合同矛盾，不能用 bridge 局部修复掩盖根路径。

本修订保持原合同，最小新增 T10 ownership 为 events.go、alignment_approval_snapshot_test.go；approval.go 已归 T10。同批其他任务无此写入范围。只隔离 Choices 与 Details 的 JSON 容器，用已有 cloneJSONValue 支持的 interface/map/slice/array；不增加通用序列化框架或承诺深拷贝任意对象图。live 副本仍共享唯一 responder；录制回放则额外移除 responder，不能混淆两者。

T10-AC04 和必需 T10-V04 race 十轮覆盖描述隔离、各 Kind exactly-once、过期/取消/零值；原 V01–V03 不缩减。T22 添加独立跨层验证步骤。既有 42 个任务、96 条要求、全部 ID、DAG、预算和公开 API 不变；当前 T10 attempt1 尚未通过 gate，原69d74f头证据保留但不能代替修复后最终头验证。C01 固定源码独立反例与复验在 findings/approval-snapshot-review。所有未通过项继续由 G03 阻挡，不回写历史 G02 报告为新结果。
