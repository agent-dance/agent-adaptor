# R018 — CodeBuddy 已声明 Agent catalog 的物化路径

T15 在R017准备真实Skill/Subagent用例时，以已接受533398e（相同缺口存在G04 2421fe4）发现：codebuddy/driver.go 的Snapshot/SyncProfile/prepareRun把Declared.Agents交给internal/profileagents，但layout/render缺少codebuddy。公开WithProfileResources(Agents)的SyncProfile在零CLI情况下返回 `profile agents are unsupported by driver "codebuddy"`。这不是provider live失败，也不能用手写文件/假catalog掩盖。

同一已固定官方CodeBuddy 2.137.1包的CustomAgentsProductProvider读取home/project agents目录的.md文件。home尊重CODEBUDDY_CONFIG_DIR；parseAgentFile读取YAML frontmatter中的name/description及正文instructions，附加字段必须按原loader逐字段核实。T15保留包SHA/提取片段、原红日志、零启动canary和最小复现，形成agent-loader-evidence.md。静态来源只证明格式，实际CLI采用仍由B06证明。

该缺口使已声明canonical Subagent observation在公开资源构造路径不可达。没有新增核心名词/执行入口/公开字段，补通既有合同。T15是唯一修复owner，精确扩展allow为internal/profileagents/agents.go、internal/profileagents/codebuddy.go、internal/profileagents/codebuddy_test.go；与B04其他owner和B05写范围无重叠。CodeBuddy渲染须局部化，保留所有旧provider分派和既有文件安全/SourcePath行为；未经官方核实的扩展字段不得暗套Claude语义或静默丢弃。

原T15验证不变，新增 `go test -count=1 ./internal/profileagents` 与 `go test -race -count=10 ./internal/profileagents`，最终SHA完整记录。独立验证公开SyncProfile及实际fake进程的Agent运行catalog/观测连接，不用SPI手注入payload代替公开构造；重跑原零CLI反例和原provider helper测试。生产错误按明确Go error返回，不把错误降级warning。

任务42、要求96、DAG/全部ID无删改，只有T15精确范围与AC/validation增加。T15仍attempt3原G03 base，R017正在执行的cold-resume/Skill/Subagent真实用例保留；G04维持重开、B05最终迁移和全部原验证规则不变。没有执行付费live/访问用户凭据/创建tag。
