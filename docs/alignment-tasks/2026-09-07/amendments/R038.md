# R038：首次完整四 provider live 后的精确返修

原8db5dbf的G05、11个CI job、T25/T26完整原生矩阵有效保留。2026-09-17用户明确授权现有登录凭据后，四provider原8命令全部执行：Claude两矩阵通过；CodeBuddy V01九PASS两FAIL、V02通过；Cursor V01六PASS一FAIL、V02通过；Codex必需模型场景失败。既有允许skip不当作模型证据。所有原47任务、96要求、45历史处置、owner/verifier及G05九依赖/真实G04基线保持。

## 确定缺陷与诊断边界

T28-F04由原T15负责：原live helper将TerminalPayload外层序列化后按正式字段解码，零CLI公开Result反例稳定失败，同一JSON正控通过。修复读取Event+JSON并保留全部旧字段和冷续接断言；普通无live tag的正负回归保证G05执行。

T28-F05由原共享层T06负责：公开SyncProfile产生的SDK managed skill link被IncludeSkills clone一概拒绝；empty/agents-only控制通过，managed-skill/full catalog失败，public Run只暴露generic不可用profile。正式manifest/source_hash/fingerprint证明、严格实际目标树和no-follow读取可作为精确例外，复制为普通目录并复用现有物化/来源语义；不得全放开symlink、删Skills复制或切换原fixture profile。未知/外部/越界/替换/嵌套link/特殊文件/来源marker冲突拒绝，源不写入，后续正式reconcile/fingerprint保持。core保留所有可取得的原错误信息。

T28-F06由原T15负责CodeBuddy GetProfile原Go error返回，避免stringify丢errors.Is/As；共享T06不写codebuddy目录。此项与T28-F05协作，但写域互斥，不引入新公共API。

T30-F05由原T17负责fixture措辞与安全诊断。原Native正控的联合谓词只知err=nil、tool callback=1，缺失哪字段未知；后续相同prompt/model单次诊断读取两资源并最终返回两nonce，但正式assistant一度将token解释为敏感凭据。这支持声明其为公开、无认证用途的合成marker，不能倒填旧失败原因。保持nonce不进prompt、所有精确输出、实际资源/回调、Native请求计数、原文件不变及MCP/Subagent事实和隔离检查。无已证生产resolver/parser缺陷，不扩展production。

T29-F03保留原失败。一次额外有界exec诊断直接观察到provider429及内部重试耗尽，无法区分配额/瞬时限流/容量；无SDK或fixture修复依据，禁止换模型/endpoint、放宽健康断言或重复刷绿。该新正文不替代旧hash-only消息。

## 独立实施与验收

T06写域仅tools_profile.go、internal/skillruntime/profile.go、新profile_clone.go与profile_clone_test.go、新alignment_managed_clone_test.go及自己的handoff。T15写域为codebuddy/driver.go、alignment_live_resume_test.go、新alignment_live_resume_terminal_test.go与profile_error_test.go及自己的handoff。T17仅alignment_live_isolation_test.go及own handoff。中央manifest/AGENTS/docs由协调者维护，原始诊断和失败不可覆盖；并发总上限6。

先确定性原反例→安全修复→非作者独立复验，再合流集中godoc/使用文档/CHANGELOG并冻结新S。原G05和全部B06同新S重新验收，8db成功不沿用。单次诊断、零CLI fixture和版本门不关闭live。G06全部依赖通过后按用户授权发起PR；merge/tag/release无授权。
