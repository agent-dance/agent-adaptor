# R047：CodeBuddy native agent 名称准入

CodeBuddy 2.155.0 的正式 `parseAgentFile` 使用 `isSafeCustomAgentPathSegment` 检查 frontmatter name；旧实现只将文件名编码，仍会成功写入官方 loader 静默跳过的名称。原 T15 负责已解析名称的 provider 私有验证，证据与精确合同见 [T15 交付](../handoffs/T15/R047-native-agent-name.md)。生产修改仅在 `internal/profileagents/codebuddy.go`；公开 API、共享资源归一化、其他 provider、依赖及 AST golden 均保持。

验证先覆盖整组 agent，再进行 reconcile；无效名称不能部分写入或删除既有健康 agent/manifest。业务 Key 可以保持不透明，只需在其默认名称不合法时显式提供合法 RuntimeName。SourcePath 内容不解析、不改写，native frontmatter 一致性仍由调用方负责。SyncProfile 返回明确错误；Run 中已经进入 Driver.Run 的准备失败保留 Infrastructure RunError、非 nil Result、原 Cause 与空 provider 审计，不能因 CLI 未启动改变错误合同。

旧错误成功预期移入明确拒绝矩阵，保留合法 Unicode、大小写、内嵌点、设备名、UI 保留名及 SourcePath 控制。保留确定性 red、首次错误测试预期以及本地 sandbox loopback 拒绝记录；具备 loopback 权限的同一命令单独留证。原四条 T15 命令及独立审查完成后才接受实施。无新增依赖。

此项与原 T28 cold exact-text/catalog deadline 失败分开记录，原 catalog 名称合法，不能将 R047 倒填为旧失败原因。补充诊断保持原 prompt/model/oracle/预算且不能代替完整原矩阵。manifest revision46 仅为 G05 添加四个生产/测试精确路径和本修订文档；原 47 任务、96 要求、45 历史、九个 G05 依赖、真实 G04 base 与所有原验证命令不变。合流后新 S 重跑 G05、11 CI jobs 和全部 B06；G06 全部通过后才发 PR，不授权 merge/tag。
