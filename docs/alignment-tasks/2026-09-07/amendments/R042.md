# R042：Luna 真实验证后的 Codex 窄域返修

用户指定使用 `gpt-5.6-luna` 并保留既有登录授权。ca315b3 原 T29 完整命令
得到 V01 30 PASS/3 FAIL/0 SKIP、V02 15 PASS/0 SKIP。后续诊断与原结果分别
留档，不以模型切换、离线反例或定向重跑关闭 T29。

- 原 T06 修复 hosted profile 冷续接：仅在完整读取既有受限快照后，证明
  官方 Codex 0.153.4 的全部 87 条系统 skills 路径、内容与默认观察 mode，才
  排除该初始化 bundle。marker、目录名、未知版本和部分内容不能授权；managed
  来源、父目录、外部资源、真实配置和权限漂移保持。无额外 scanner、CLI 初始化
  或 legacy 双 hash。旧已含完整 bundle 的 record 保守拒绝且保持健康状态。
- 原 T16 的正式协议职责保持；root 补充修正句末 `$review.` 的 typed skill
  选择，精确 dotted 名称优先、歧义不回退。它不证明实际执行或解释缺少 plan。
- Codex app-server 只将正式声明且直接父 thread 匹配、无身份冲突的 child
  `thread/status/changed` 作为 Raw-only 控制消息。其他 foreign thread/turn、
  未知 child 和 malformed 状态仍拒绝；canonical subagent completion 保留
  child role、当前 spawn receiver 与唯一 catalog 的既有证明。
- 成功 MCP 的 absent/null error 不标记 Transcript.IsError；实际 error 或
  failed status 保持错误。Raw、terminal、正式事件和同次 Transcript 不缩水。

原完整 observation 场景缺少正式 plan 的原因仍未确认。不得猜测 Todo、削弱
prompt/断言、增加重试刷绿，或把其它修复的离线通过当作 plan live 通过。

新增源/测试由原 owner 的隔离分支交付，非作者独立复验；中央 godoc、使用文档、
CHANGELOG 与精确 G05 路径同步。原 47 任务、96 要求、45 历史项、G04 基线、
G05 九依赖与全部原命令不变。最终新 S 必须重新执行 G05、原生平台及全部
B06；Cursor 原 DNS/live 失败继续保留，G06 未通过前不宣告可合入。PR 在完成
后发起；不授权 merge、tag 或发布。
