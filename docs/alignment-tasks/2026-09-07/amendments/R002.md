# R002：RunError 原因保留的 A2A 消费者回归防护

修订3，2026-09-07，G00依据C02代码审阅补充T03。

基线 bridges/a2a/server.go executor.Execute 和 hosttools/a2adelegation/local.go finalTask 都先判断 errors.Is(context.Canceled)，会把携带取消cause的审批拒绝/超时判成取消，并跳过RunError.Result。这是B01 T05保留Cause后必须同批关闭的消费者回归。

T03增补两个bridge文件与验收T03-AC04。实现只消费基线已存在的RunError/ReasonCancelled；用errors.Join的fake Runner/Stream验证，无同批Go符号依赖。所有原95要求、任务ID与依赖保留。完整delegation审批/预算failure code传播仍属于T18，不能把这次主状态/部分输出修复宣称为W13全部完成。

同批T01、T02、T04、T05、T31无路径交集；通过完整任务包校验后随G00冻结。
