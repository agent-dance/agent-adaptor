# R032：完整合入审查的两个补充修复

本修订保留 R030 六路返修与 R031 全部新增工作项；追加原 T18 的 MERGE-F02 补充返修。原47任务、96要求、45提交处置、全部原 owner/verifier、G05九依赖和完整历史G04基线不变。七个累计实施owner不是七路同时运行：补充任务只在已有slot空闲后派发，实施/独立审查总并发上限仍为6。

## MERGE-F01 合法协议证据澄清

独立审查补充的严格 fixture `fallback_contract_test.go` SHA256 06bf77457a7d2e9532279af111b521edf131c5d7c29d147319dc0a448bde02dd 及 `fallback_contract_metadata_test.go` SHA256 25b3f6fac2f9d61c02712f0af1449a07afbf5971ab8187b5f878744ed182b8e4 使用合法 run.error/Error、零Sequence/Seq/Timestamp和完整fresh文本生命周期。候选646a26d仍丢fresh文本并保留旧provider坐标/Source；919f140基线同主体通过。合法失败terminal下Usage=13、唯一公共生命周期及Meta.RunID是通过的控制，不得夸称旧Usage是已证缺陷。初期非合同压力fixture和日志保留单列；T06以严格协议形状修复、原reviewer独立验收。

## MERGE-F02 polling部分结果必须保留制品数量限制

独立审查发现 polling 的 ctx.Done 新部分结果路径未调用既有 limitResultArtifacts，而正常完成、stream取消和poll耗尽路径均调用。公开 MaxArtifacts 指向0时，远端Working任务已有两个artifact再取消，候选仍返回两个compact Artifacts，违反已发布限制。RemoteArtifacts的显式完整保留不受该compact限制，不能以删除完整审计修正。原artifact_result_limit事件、错误主因/可匹配cause、有界CancelTask和部分结果均须保持。

原T18在外部dispatch指明的当前集成基线独立worktree实施，kind=rework_attempt。此次窄域仅 `hosttools/a2adelegation/delegator.go`、新增 `hosttools/a2adelegation/alignment_polling_artifact_limit_test.go` 与原own handoff；协调者拥有任务和中央docs。保持所有原V01、race×10 V02，新增公共Delegate的取消/超时、MaxArtifacts=0/正数/nil、可观察drop、完整RemoteArtifacts和主因断言。由原非作者review_bridges复验，不自签验收。

G05精确新增上述test及T06 `alignment_resume_fallback_test.go` 整合路径；其余生产路径已获原scope授权。最终源码/文档S必须包括这些修复且通过G05、全部原生平台与四provider原完整矩阵，旧通过不沿用；merge/tag/release仍另行授权。
