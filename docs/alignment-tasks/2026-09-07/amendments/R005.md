# R005：有效Ask与精确schema矩阵一致

修订6，来自T31首轮提交5433604535f6a6e0aa5a3d235b6c00ffe14a9beb的独立代码审阅T31-F01。首轮640项普通/245项race测试通过，但fake Driver未实际发审批请求，未发现有效默认值漏口；首轮证据保留，T31返修attempt2。

证据：internal/engine/structured.go仅识别显式Ask；sink.effectiveApprovalPolicy与driver.EffectiveHumanDecisionPolicy把unset Permission/PlanReview解析成Ask。QuestionAsk+Permission留空时，NativeHITL.Permission=false也会被选中，真实控制请求随后进入Permission Ask。

冻结补充裁决覆盖C04相关矩阵定义：

- 每个nonnull精确矩阵必须按完整EffectiveHumanDecisionPolicy匹配全部有效Ask，并符合普通RunPolicyCaps.Ask；包括unset继承的Permission/PlanReview。
- 每个nil矩阵保留已发布的显式Ask+WorksWithHITL规则；不因另一个机制使用新矩阵就全局改变legacy边界。
- transport候选按对应机制判断，不能以batch fallback消音其适用Ask；候选过滤只改局部值，不修改共享Descriptor。source仍由唯一resolver按native优先/合法prompt fallback决定。
- 无schema的默认审批行为不变。新增精确矩阵可能使此前未明确Permission策略的schema调用改走prompt或unsupported；T07须真实说明这一点，不能偷偷自动批准以维持native。只声明Question/PlanReview native不等于QuestionAsk+其他Kind unset一定native。
- T31-AC04使用真实DecisionCapableSink的审批往返验证，T22独立复核；不能用不发审批的fake结果声称支持。普通零policy、新矩阵、legacy nil、显式自动策略和mixed Ask均覆盖。

原95要求、42任务、7批次及所有scope不变。W06-R03仍归T31；此finding关闭前G01不得放行。G00旧报告不倒改，G01统一提交修订与新的实际验收证据。
