# P5.2 大挪移·侦察清单与执行裁定

> 来源：只读侦察 agent（2026-07-27，基于 `a0fe728` 工作树）；这是文件/符号迁移清单，不代表当前实现状态。
> 当前执行状态、阻断项和顺序以 [v1-takeover-audit.md](./v1-takeover-audit.md) 为准；本文仅在其授权 MOVE/DELETE 后作为机械执行依据。
>
> **接管状态**：HEAD 为 `4a66cc3`，工作树含未提交 PRE 改动；这些改动测试曾通过，但尚未由接管者完成逐项 diff 审计、独立验收和提交，因此 PRE 不得标记完成，MOVE 不得开始。

## 0. 执行裁定（协调者）

按风险节点逐条裁定。2026-07-27 的机械复核证明原四波方案漏算根残留与旧根消费者，§0 的旧数量仅作历史侦察证据；当前波次以本节修订为准，每波结束都必须全仓 build/vet/test 绿：

| # | 议题 | 裁定 |
|---|---|---|
| D-P5.2-1 | 撞名与步骤序（§9、§10.1） | **ROOT CUTOVER 原子提交**：先经 REHOME/REPOINT 与 LEGACY EDGE DELETE 清空旧根生产接缝；再删除全部剩余旧根 Go 文件、搬入当时完整的 `next/`、清除全部 `/next` import。同一提交根目录只能有 `package adaptor/adaptor_test`。原“删 28 + 搬 40 + repoint 61 ≈107”遗漏 10 个根残留和大量旧根 import，数量作废，cutover 前动态重建。 |
| D-P5.2-2 | 驱动 Config 类型族（§10.3） | **各驱动包自有真结构体**：claude/codex/cursor/codebuddy 各自定义真 `Config` struct（字段与 engine.XConfig 一致），`Driver(cfg)` 内部逐字段转换成 engine 类型。公共 API 不暴露 internal 路径、不保留根包别名。CodeBuddyPermissionMode 等驱动专属常量随各自包走。 |
| D-P5.2-3 | Profile 资源族（§10.3） | **REHOME/REPOINT 下沉 `driver/`/`profile/`**：公共真身必须位于公开叶子包，engine 消费公开合同；同时覆盖 `next` 的 Profile/Resource aliases 与 `profile.Resources`，不是只搬原清单类型。 |
| D-P5.2-4 | HITL typed 族（§10.3） | **REHOME/REPOINT，不入根公共 API**：v1 消费面是 `ApprovalRequest`；legacy typed request/handler 留 engine 内部，claude/codebuddy 生产调用点直接 import engine，再删根 alias。 |
| D-P5.2-5 | 根包真函数下沉（§10.4） | `EffectiveHumanDecisionPolicy` → `driver/`（只操作 driver.HumanDecisionPolicy，自然归属）；4 个 `SessionParam*` 常量 → `driver/` 导出（顺带解 P4.9 可用性缺口⑨）；`NormalizeProfileDir` → `internal/engine`。 |
| D-P5.2-6 | init 注入接缝（§10.2） | **消灭注入**：LeaseTTL/LeaseRenewInterval 纯常量，直接内联进 engine；默认 skill 物化器**真身移入 internal/engine**（zip/tar/tgz 解压是运行时机器，不是词汇），删除 `DefaultSkillMaterializerFactory` 工厂变量与 `engine_wiring.go` 的 init()。`skill/` 只保留构造词汇（SkillFromArchive/ArchiveFrom* 真身迁入 skill/），物化器选项（WithSkillCacheRoot 等）在 skill/ 包装 engine 导出构造器。做完跑一次「不 import 根包、只用 skill.Archive」的最小程序验证不静默失败。 |
| D-P5.2-7 | archive 自冲突 bug（§10.9、P3.2 待办④） | skill 翻转波顺修：`internal/engine/skill_helpers.go` `skillSourcesEquivalent` 补 `SkillFromArchive` 分支 + 回归测试（default 作用域同 archive 技能不再自冲突）。同波删根包 `skillMerger`/`skillBucket` 死代码（勿动 engine 同名真身）。 |
| D-P5.2-8 | adaptertest/v1（§6.3） | 采纳上提：删旧 adaptertest 2 文件 → `v1/*` 上提 `adaptertest/` → 4 个 conformance 测试去 `/v1` 别名。 |
| D-P5.2-9 | clients/a2a 边界测试（§10.5） | 保持现状不放宽 forbidden 列表；若某波需要 clients/a2a 引模块内包（预计不需要），单独上会裁定。 |
| D-P5.2-10 | 双 import 同址包（§10.6） | bridges/hosttools/appserver 7 个包**按包逐个原子处理**（一个包的新旧合一在一次编辑内完成），不跨包混改。 |
| D-P5.2-11 | examples 改名（§8） | RENAME 波执行 14 项映射 + web-chat 三合一 + 新建 structured-output 示例 + run_examples.ps1/README/6 处活 docs 同步 + 修 README 3 条死链；历史 workstream 文档不改。 |
| D-P5.2-12 | §10.8 showcases 未 track | **已失效**：0228dbd 已提交 showcases/team-agent-workflow（450+311 行）。ROOT CUTOVER 前的动态 repoint 清单必须包含它。 |

**修订后的可编译波次**（顺序执行，变更波独占写入面）：

1. **PRE 波（当前 dirty，待验收）**：仅 D-P5.2-2/5/6/7；完成逐项 diff 审计、定向回归、全仓 build/vet/test 后形成独立提交。D-P5.2-3/4 已移出本波。
2. **CORRECTNESS 门禁**：关闭 takeover audit 的 G 项、P4.9、SPI 与 bridge 保真缺口，收敛唯一 invocation/thread/driver/checkpoint/result/Event 管线。
3. **REHOME/REPOINT**：D-P5.2-3/4，加上 `next`/`skill`/`profile` 暴露的全部 internal aliases；四 Driver、appserver、internal 生产消费者脱离旧根 aliases。
4. **LEGACY EDGE DELETE**：在旧根仍可编译时按包删除/替换旧 bridge、hosttool、memory、provider sugar、旧 adaptertest 和 `pkg/` forward。
5. **ROOT CUTOVER（D-P5.2-1，原子）**：动态生成文件/import 清单；删全部剩余旧根 Go、搬完整 staging、清 `/next` import。结束态根 import path 即 v1。
6. **RESIDUAL DELETE**：清除 cutover 后无引用 aliases、metadata 兼容与死代码。
7. **RENAME**：§9 步骤 3 + D-P5.2-8/11；wire schema 中真正的版本号不因 Go 临时后缀清理而改名。

---

## 1. 根目录 .go 文件分类（41 个：19 非测试 + 22 测试）

### 1.1 非测试文件（19）

| 文件 | 行数 | 类 | 依据 / 去向 |
|---|---|---|---|
| `api.go` | 118 | A + B 混合 | `SDK`/`Runner`/`RunHandle` 三接口被 `next/agent.go` Agent/Runner + `next/stream.go` Stream + `next/result.go` Result 取代 → 删。尾部 6 个 engine 别名 `AdminAPI`/`AgentAdmin`/`AgentBinding`/`TypedAgentBinding[T]`/`AgentDefaults`/`AgentInfo` 仅被四驱动包 v0 `New()` 用，随 v0 `New()` 删 |
| `sdk.go` | 74 | A | `sdkFacade`/`Build`/`New`/`Default`/`Agent`/`Admin` → `next/wiring.go:50 New` |
| `runner.go` | 1239 | A | `runnerImpl`/`dualSink`/`asyncRunHandle`/`seqSink`/`noopEventSink` → `next/thread.go`+`next/sink.go`+`next/stream.go` |
| `options.go` | 541 | A | v0 全部 `With*` → `next/options.go`（D7 三分） |
| `errors.go` | 237 | A | sentinel + 别名 + `Is*` → `next/errors.go`（D1 `*RunError`） |
| `structured_output.go` | 345 | A | → `next/structured.go` `WithSchema[T]`/`RunAs[T]` |
| `run_policy.go` | 78 | A | → `next/policy.go`。**例外**：`EffectiveHumanDecisionPolicy`(:52) 被 `claude/parser.go:979,1368,1565,1930`、`codebuddy/control_parser.go:197,212`、`codebuddy/engine.go:44` 非测试调用 → 先下沉 driver/（D-P5.2-5） |
| `profile.go` | 70 | A | → `profile/` + `next/options.go`。**例外**：`NormalizeProfileDir` 被 `internal/skillruntime` 用 → 下沉 engine |
| `binding.go` | 18 | A | `Bind`/`BindTyped[T]` v1 无对应物；四驱动包 `driver.go` v0 `New()` 与之同生共死 |
| `doc.go` | 19 | A | → `next/doc.go`。文件名撞 next/（§10.1） |
| `driver_aliases.go` | 567 | A（整文件删） | 316 个 `= driver.X` 别名；下游改直接 import driver/ |
| `engine_aliases.go` | 184 | B（必须迁移） | 96 个 `= engine.X` 别名，最大阻塞点，见 §3.3 与裁定 D-P5.2-2/3/4 |
| `engine_wrappers.go` | 97 | B | `WithCallerIdentity`/`CallerIdentityFromContext` → `next/identity.go`；`SessionParam*` 4 常量被四驱动包非测试引用 → driver/（D-P5.2-5）；`SessionCodecFor`/`Key`/`LocalSkill`/`FSSkill`/`InlineSkill`/`Require` 被 `skill/skill.go` 与 `adaptertest/adaptertest.go` 引用 |
| `engine_bridge.go` | 69 | A | 19 个未导出 delegate，仅服务 runner.go/options.go，随之删 |
| `engine_wiring.go` | 25 | B（关键接缝） | init() 注入 LeaseTTL/LeaseRenewInterval/DefaultSkillMaterializerFactory → 按 D-P5.2-6 消灭 |
| `archive_source.go` | 219 | B（真身，须搬） | `SkillArchiveFormat`/`SkillFromArchive`/`ArchiveFrom{Bytes,Path,URL}`/`ArchiveHTTPOption`/`WithArchiveHeader`/`WithArchiveHTTPClient`/`sniffArchiveFormat` → skill/ |
| `archive_materializer.go` | 384 | B（真身，须搬） | `DefaultMaterializerOption`/`WithSkillCacheRoot`/`WithMaxArchiveSize`/`WithMaxFileSize`/`WithMaxArchiveEntries`/`NewDefaultSkillMaterializer` + zip/tar/tgz 解压 → 按 D-P5.2-6 拆：解压机器进 engine、选项词汇进 skill/ |
| `skill_dirscan.go` | 198 | B（真身，须搬） | `DirScanOption`/`WithDirSkillKeyPrefix`/`WithDirIgnore`/`WithDirSkillFile`/`LocalSkillsFromDir`/`SkillsAsRefs` → skill/ |
| `skill_helpers.go` | 380 | B 部分 + A 死码 | 保留迁移：`newDefaultSkillMaterializer`(:37)、`defaultSkillMaterializer` 实现、`SkillCacheRootEnv`(:46)。删：`skillMerger`/`skillBucket`(:276–380)——engine/skill_merger.go 已有同形真身，根包这份零调用点（P0.2 遗留死码；勿误伤 engine 侧同名 `sourceLabel`） |

### 1.2 测试文件（22）

- **A 类（18）**：`admin_profile_test.go` `mcp_sdk_test.go` `run_contract_test.go` `run_model_override_test.go` `run_policy_test.go` `runner_decision_test.go` `runner_hitl_integration_test.go` `runner_session_internal_test.go` `runtime_admin_test.go` `sdk_session_test.go` `sdk_start_session_test.go` `skills_sdk_test.go` `stream_internal_test.go` `structured_output_test.go` `errors_test.go` `profile_resources_test.go` `caller_identity_test.go` `session_codec_internal_test.go` —— 被 next/ 22 个测试（含 8 个 scenario）取代。
- **B 类（3）**：`archive_source_test.go` `archive_fuzz_test.go` `skill_dirscan_test.go` 随真身迁移。
- **B 类（1）**：`skill_contract_test.go`（6 处 `SkillCacheRootEnv`）对照 `next/skills_contract_test.go` 裁剪后并入。

**C 类核实为空**：41 个 .go 全部 `package agentadaptor`/`agentadaptor_test`。

## 2. next/ → 根目录搬迁映射

- **包名**：next/ 全部 40 文件已是 `package adaptor`/`adaptor_test`；搬迁零改包名（D3：包名 adaptor ≠ 目录名 agent-adaptor，合法）。
- **撞名 3 个**：`doc.go` `errors.go` `options.go`（`comm -12` 核实）；测试文件名零交集。按 D-P5.2-1 同波删旧直落。
- **其余 14 非测试 + 23 测试同名直落**：agent/approval/events/identity/inspect/policy/result/runservices/sink/stream/structured/thread/threadstore_engine/wiring。
- **next/ 非测试 import 面**：仅 driver、internal/engine、mcp、profile、threadstore + stdlib + invopop/jsonschema。搬后需保证词汇包不反向 import 根包（现状违例见 §3.4）。
- **import repoint 全量**：`grep -rl 'agent-adaptor/next"'` → **61 文件**（next/ 内 22 自测 + 39 外部）。全部用 `adaptor "…/next"` 别名形式，机械去 `/next`。39 外部 = bridges/a2a 4、bridges/agui 2、bridges/sse 2、bridges/subagentstream 2、hosttools/a2adelegation 7、hosttools/sessionrecorder 2、examples 20（含 showcases/team-agent-workflow，已 track）。
- ⚠️ 其中 bridges/agui、bridges/a2a、bridges/sse、bridges/subagentstream、hosttools 两包**同时** import 根包旧 API，搬后同包指同路径 → 符号层冲突，按 D-P5.2-10 逐包原子处理。

## 3. 仍引用根包旧 API 的站点

Import 根包的文件数（按目录）：claude 27（12 非测试）、codebuddy 21（13）、codex 17（11）、cursor 15（7）、根自身 14（测试）、internal/mcpruntime 12、bridges/agui 8、next 6（仅测试）、hosttools/sessionrecorder 6、bridges/a2a 6、internal/skillruntime 5、codex/appserver 5、bridges/sse 5、skill 3、internal/profileconfig 3、providers 2、memory 2、internal/profilekind 2、internal/profileinstructions 2、internal/profilehooks 2、internal/profileagents 2、internal/clihelper 2、internal/adapterutil 2、hosttools/a2adelegation 2、bridges/subagentstream 2、profile 1（仅测试）、internal/testutil 1、internal/profilesnapshot 1、clients/a2a 1（字符串常量非真 import）、adaptertest 1。

### 3.1 四驱动包（共用 224 个根符号）

- **→ driver/ 直改（约 150 个）**：DriverAdapter/DriverDescriptor/DriverRunRequest/DriverRunResult/DriverSessionState/DriverCheckpoint/EventSink/DecisionCapableSink/Stream* 全族/Transcript* 全族/RunEvent*/HookEvent*/Instruction*/MCP*/Skill*/Profile{Mode,Selection,Payload}/*Capability/Isolation*/Failure*/HumanDecision*/Question*/Decision*/Usage/QuotaReport/ModelInfo/EnvBinding/WorkspaceLease 等。
- **→ engine 别名 44 个（无公开新家，按 D-P5.2-2/3/4 处置）**：Config 族（CommonConfig/ClaudeConfig/CodexConfig/CursorConfig/CodeBuddyConfig/CodeBuddyPermissionMode+7 常量/ThinkingEffort）；HITL 族（PermissionRequest/Response、PlanReviewRequest/Response、QuestionRequest/Response、ApprovalApproved/Rejected、QuestionAnswered）；Profile 资源族（ProfileKind/ProfileKindHostManaged/ProfileResources/ProfileSnapshot/ProfileResourceKind/ResourceSnapshot + 5 个 ProfileResource{Skills,MCP,Agents,Hooks,Instructions,Config} + 5 个 Support* + 3 个 Materialization*）；SessionRequest；TypedAgentBinding。
- **→ 根真声明的非测试调用点仅**：`BindTyped`（四包 v0 New()）、`EffectiveHumanDecisionPolicy`（7 处）、`SessionParam*` 4 常量。其余 v0 符号只在 _test.go 或 godoc 注释（非测试 `agentadaptor.` 共 1125 处中注释仅 8 处）。
- **侦察时快照（PRE 前）**：各包 `driver_v1.go` 导出 `type Config = agentadaptor.XConfig` + `func Driver(cfg Config) driver.Driver`；exampleutil 已全走此路线。当前 PRE 正按 D-P5.2-2 将 `Config` 改为真结构体，但仍须独立验收；v0 `New()`/`NewAdapter()` 可在 DELETE 波整体删除。

### 3.2 bridges / hosttools 双栈包

| 包 | v0 文件 | 主要旧符号 |
|---|---|---|
| bridges/agui | bridge.go events.go input.go + 5 测试 | RunHandle RunResult RunEvent RunFailure StreamPayload Stream* Decision* HITL*Payload HumanDecision* Role Usage WithStreaming ErrRunEnded |
| bridges/a2a | mapping.go server.go types.go stream_status.go + 2 测试 | SDK Bind New Runner RunHandle RunResult RunOption WithDefaultAgent WithSessionKey WithStreaming WithDefaultStreaming WithoutStreaming Stream* Transcript* StructuredOutput |
| bridges/sse | decision.go handler.go + 3 测试 | SDK Bind New RunHandle RunResult RunOption Err{DecisionRequestExpired,DecisionResultKindMismatch,RunEnded} WithSessionStore Stream* |
| bridges/subagentstream | mux.go + 1 测试 | RunHandle RunResult RunEvent RunFailure StreamPayload StreamRun{Started,Finished,Error} Decision* ErrRunEnded |
| hosttools/sessionrecorder | pending.go recorder.go + 4 测试 | StreamPayload StreamHITL* Decision* HITL*Payload HumanDecision* |
| hosttools/a2adelegation | status_decoder.go + mapping_test.go | 仅 Stream* 常量族 + RoleUser → 全 driver/ 别名，最轻 |
| codex/appserver | run.go translate.go + 3 测试 | CodexConfig CommonConfig RunPolicy HumanDecisionPolicy Isolation* New WithDefaultAgent With* Driver* Stream* |

### 3.3 p0-inventory 盲点核查 11 个 internal 包 —— 11/11 仍成立

| 包 | 文件数 | 符号族 | 去向 |
|---|---|---|---|
| internal/adapterutil | 2 | AgentIdentity EnvBinding Environment* Runtime* | 全 driver/ |
| internal/clihelper | 2 | EnvBinding EventSink RawStreams RunEvent* StreamPayload | 全 driver/ |
| internal/mcpruntime | 12 | AgentProfile MCPPayload MCPServerSpec MCPTransport* ProfileKind* ProfileResource* ResourceSnapshot ErrInvalidMCPConfig ErrMCPTransportUnsupported | 混（Profile 族按 D-P5.2-3 → driver/） |
| internal/profileagents | 2 | AgentPayload AgentSpec AgentToolPolicy Hook* ProfileResource* ResourceSnapshot | 混 |
| internal/profileconfig | 3 | ConfigField NativeConfigPatch ProfileConfig* ProfileResource* ProfileSnapshot ResourceSnapshot | 混 |
| internal/profilehooks | 2 | 22 个 HookEvent* + Hook* + ProfileResource* ResourceSnapshot | 混 |
| internal/profileinstructions | 2 | Instruction* InstructionsBundleRef ProfileResource* ResourceSnapshot | 混 |
| internal/profilekind | 2 | AgentProfile ProfileKind ProfileKindHostManaged ProfileKindShared | Profile 族 |
| internal/profilesnapshot | 1 | 26 个 payload/snapshot 族 | 混 |
| internal/skillruntime | 5 | 35 个含 NormalizeProfileDir（根真声明） | 混 + D-P5.2-5 |
| internal/testutil | 1 | RunEvent StreamPayload | driver/ |

不 import 根包的 5 包（aguiversion configprobe processx profilereconcile profilestate）已核实。8/11 包共用 ProfileResource*/ProfileSnapshot/ResourceSnapshot 族——D-P5.2-3 的主要受益面。

### 3.4 词汇包反向依赖

- `skill/`：**非测试反向 import 根包**（39 符号，archive/materializer/dirscan 真身 + engine_wrappers 真函数 + options.go 的 With* ）→ PRE 波翻转。
- `profile/`：仅 profile_test.go 反向（非测试干净）。
- `memory/`：session_store.go(+test) 是 v0 SessionStore 实现 → A 类删；store.go 是 v1 留。
- `mcp/`、`threadstore/`：已核实零反向。
- `adaptertest/`：adaptertest.go 38 个根符号 → 被 v1 取代，DELETE 波删。
- `providers/`：唯一引用者是自己的 test → 零外部依赖，无痛删（D9）。

### 3.5 runtimeservice/（D10）
已核实为空：目录不存在、全仓无 import。P4.5 已删，P5.2 无需处理。

## 4. pkg/ 转发包

7 包各 1 个 forward.go（86/44/30/24/83/82/44 行），全带 Deprecated 注记。Go 引用点仅 2：`claude/agui_conformance_test.go:12`、`codex/appserver/agui_conformance_test.go:10`（均引 pkg/bridges/agui）。文档引用 6 处：docs/streaming.md:78,106、workstream-session-recorder.md:199,403、workstream-streaming-chat.md:237,250、p0-inventory.md:153,158。`pkg/clients/a2a/forward.go` 不 import a2aproject（已核实），删除对 clients/a2a 边界测试无影响。

## 5. 旧 metadata key 兼容解析残留

- **唯一真解析点** `internal/engine/mcp.go`：godoc :116；解析键 :129-134（key/transport/command/url/bearer_token_env_var/required_reason）、:157(args_json)、:160(env_json)、:163(headers_json)、:166(required)、:206(enabled)。
- 仅 godoc 提及（改文案）：`driver/runtime.go:86`、`hosttools/a2adelegation/option.go:109`。
- 测试夹具随删：`runtime_admin_test.go:209-215,339-341,409-412,430-431,454-455,481-482,494-495`；`internal/adapterutil/helpers_test.go:44-45`。
- **勿删**：`bridges/a2a/convert.go:170` `"agentadaptor.failure"` 是 A2A 线上格式 key。

## 6. V1 后缀 → 终名

### 6.1 需改名 8 组（全部与同波删除的旧名一一对应，零意外撞名；删除先于改名）

| 现名 | 位置 | 终名 | 冲突源（同波删） |
|---|---|---|---|
| sse.HandlerV1 | bridges/sse/handler_v1.go:80 | sse.Handler | handler.go Handler |
| sse.OptionsV1 | handler_v1.go:55 | sse.Options | handler.go Options/Protocol/RawRequest |
| a2a.NewServerV1 | server_v1.go:149 | a2a.NewServer | server.go NewServer |
| a2a.ServerV1 | server_v1.go:136 | a2a.Server | server.go Server |
| a2a.ServerOptionsV1 | server_v1.go:97 | a2a.ServerOptions | types.go ServerOptions |
| a2a.StatelessV1 | server_v1.go:59 | a2a.Stateless | types.go Stateless |
| a2a.PromptBuilderV1 | server_v1.go:87 | a2a.PromptBuilder | types.go PromptBuilder/Func |
| a2a.ResultBuilderV1 | server_v1.go:92 | a2a.ResultBuilder | types.go ResultBuilder/Func |

已是终名：a2a.ThreadByContextID、a2a.SessionBinding。同波删无替身：a2a.SessionByContextID/SessionByTaskID/SessionMapper(Func)。

### 6.2 勿动（线上协议版本号）
`bridges/a2a/stream_status.go`：AdapterStreamSchemaV1（"adapter.stream.v1"）/AdapterStreamEnvelopeV1/AdapterStreamEventV1。

### 6.3 adaptertest/v1 → 上提 adaptertest/（D-P5.2-8 已采纳）
v1/ 7 文件已是 `package adaptertest`、只 import driver+stdlib；引用方仅 4 个 driver_v1_conformance_test.go。旧 adaptertest.go+doc.go 被 4 个 conformance_test.go 引，随 v0 New() 删。唯一撞名 doc.go，删旧后落位。

## 7. engine 接缝清理

- 现有接缝：`nextwiring.go`(213) 16 个导出薄包装 + `threadsession.go`(125)，消费者仅 next/。
- 搬迁后可收敛：legacy 委托者（(*Core).resolveSkills/collectAdminCandidates/(*Core).prepareRuntime）随 runner.go/sdk.go 删除失去调用者 → lifted 函数与薄包装可塌成一层；ProfileResourceDriver 别名与未导出接口可合一。
- **保留**：ResolveWorkspaceLease/ReleaseWorkspaceLease/ReleaseRuntimeServicesByRun 是纯 nil-guard，留 engine 侧。
- **恒久约束**：`internal/engine` 永不 import 根包（根包 import engine 单向）。收敛接缝时不得破坏——nextwiring 全部只收发 driver.* 与 engine 自有类型正是此约束产物。

## 8. examples 改名/合并映射

现状 16 项（含新 showcases/）。映射：codex-basic→quickstart；codex-admin-named→并入 inspect；codex-sessions+session-codec-inspect→threads；codex-skills-live→skills；codex-profile-full+profile-resources→profiles；codex-stream+streaming-chat→streaming；streaming-sse-server+streaming-chat-aguiclient+streaming-chat-copilotkit→web-chat/（分子目录 sse/aguiclient/copilotkit；copilotkit 是唯一带 3 个 _test.go 的，均 package main，合并时须处理）；a2a-local→a2a-server；showcases/team-agent-workflow 同名保留；新建 structured-output（S5 一屏示例）。

同步点：run_examples.ps1 9 处（:122-133，-Name 参数与目录名已不一致需一并顺）；README.md:234-246 13 行（:239/:241/:242 三条死链一并修：mock-runtime-admin/mock-adapter-playground/mock-skills-contract 不存在）；活 docs 6 处（a2a.md:81、streaming.md:69,139、usage-guide.md:196、profile-resource-provider-matrix.md:42-44、workstream-profile-resources-*.md:88-90,641）；历史 workstream 文档 ~25 处不改；CI 只跑 ./... 零影响。

## 9. 原三步机械迁移序（须服从 §0 与 takeover audit）

- **历史估算（已作废）**：侦察时曾认为最小原子是删 v0 核心 28 文件 + 搬 next/ 40 文件 + repoint 61 import ≈107；机械复核发现还会留下 10 个旧根 Go 文件并漏掉旧根消费者，不能据此执行。
- **历史 DELETE ~200 估算（已作废）**：D-P5.2-3/4 与生产 aliases 消费方必须提前在 REHOME/REPOINT 解序；ROOT CUTOVER 后只允许 residual delete。
- **步骤 3** 纯机械改名（= §0 RENAME 波），名字已被步骤 2 腾空。

## 10. 机械迁移风险（合同风险另见 takeover audit）

1. 根↔next 撞名 3 文件 → D-P5.2-1 同波删旧直落。
2. engine_wiring.go init 注入断裂（最高危）→ D-P5.2-6 消灭注入 + 最小程序验证。
3. engine_aliases 44+ 类型无公开新家 → D-P5.2-2/3/4。
4. EffectiveHumanDecisionPolicy/NormalizeProfileDir/SessionParam* 真依赖 → D-P5.2-5。
5. clients/a2a 边界测试禁 import 模块内一切包 → D-P5.2-9 保持现状。
6. 双 import 同址符号层冲突 → D-P5.2-10 逐包原子。
7. engine 反向依赖不可放大（恒久约束）→ §7。
8. showcases 未 track → 已失效（0228dbd 已提交）。
9. skillMerger 死码 + archive 自冲突 bug → D-P5.2-7。
10. staging 第二套执行管线、checkpoint/session/fingerprint、取消背压、Result/Inspect/App-server 保真等阻断级合同问题不属于本侦察清单；统一由 takeover audit 跟踪，关闭前禁止 MOVE。
