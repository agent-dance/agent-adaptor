{
  "name": "agent-adaptor-web-agui",
  "version": "0.1.0",
  "private": true,
  "description": "Vite + React + @ag-ui/client HttpAgent — browser talks AG-UI directly to the Go backend, no CopilotKit.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@ag-ui/client": "latest",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  },
  "engines": {
    "node": ">=20"
  }
}
